# Optimizer tick — 2026-07-28 06:00 UTC

## Run metadata
- Tick ID: `b7529bce-ad71-48c4-bbbc-efd595019ba6`
- Occurred at: 2026-07-28 06:00:09 UTC
- Mode: `shadow`
- Duration: 2,487 ms
- Data pull OK: yes
- Variants seen: 10
- Ads seen: 45
- Signups seen: 2

## Decision counts
| Decision | Count |
| --- | ---: |
| NO-OP | 10 |
| PAUSE | 0 |
| SCALE | 0 |
| REMIX | 0 |

## Summary
Awaiting data — every one of the 10 variants tripped the "insufficient data" guard (visits=0; thresholds are ≥100 visits and ≥5 conversions per variant before any PAUSE/SCALE/REMIX can fire). Posterior spread across variants is tight (`p_best` roughly 0.096–0.107), which is exactly the flat-prior state we expect this early.

Note the mismatch worth watching: the tick recorded `signups_seen=2` and `ads_seen=45`, but per-variant `visits` is 0 for every row. That likely means signups are being attributed at the account/tick level while per-variant visit attribution isn't landing yet (or the visit-tracking pixel isn't reporting into the optimizer's per-variant view). Not urgent on day-1 of the shadow window, but if visits stay at 0 across the next 2–3 ticks while `signups_seen` keeps climbing, the attribution pipeline needs a look — otherwise the optimizer will sit in NO-OP forever regardless of real performance.

## Shadow decisions for review
None. No non-NO-OP decisions were produced this tick, so there is nothing that would have been enacted in live mode.
