# Daily content cron — 2026-07-05

## Status

- **FB cron** (`/api/social/fb-daily-generate` @ 14:00 UTC): **healthy**.
- **IG cron** (`/api/social/ig-mirror` @ 14:15 UTC): **silent failure — chronic outage day 39** (last successful IG publish: 2026-05-27).

## Today's row

| field | value |
|---|---|
| `id` | `c6aa93f4-39db-465b-8922-2337d5a63a42` |
| `slug` | `gen-2026-07-05-llo0i` |
| `angle_tag` | `pumpkin-patch-hayride-boots-rec` |
| `display` | She named the boots \n on the hayride bench. |
| `status` | `fb_published` (stuck — should be `ig_published`) |
| `fb_post_id` | `1086292991223812_122116124714989743` |
| `fb_published_at` | 2026-07-05 14:01:29 UTC |
| `ig_media_id` | `null` |
| `ig_published_at` | `null` |
| `error_message` | `null` (silent failure — the bug) |
| `errored_at` | `null` |

## Signal

- Vercel edge-middleware logged `14:15:08 GET /api/social/ig-mirror 200`.
- No serverless-function output, no errors in `get_runtime_errors` for `/api/social/ig-mirror`.
- No DB update — the row was not marked `ig_failed`.
- This exactly matches the `fetchPageContext`-swallowed-error signature that PR #143 is designed to surface.

## Backlog

- **33 rows** stranded at `status='fb_published'`, `ig_published_at IS NULL`.
- Range: `2026-05-29` → `2026-07-05` (39 days).

## Action taken today

- **No new PR opened.** 25 duplicate observability-fix PRs (#117, #118, #119, #121, #122, #125, #126, #127, #128, #133, #134, #136, #138, #139, #141, #142, #143, plus health-log PRs #124, #129, #130, #135, #137, #144, #146) are already open. Opening a 26th would add noise, not signal.
- Holding the "no duplicate" line from the 2026-07-02 and 2026-07-04 supervisor logs.

## What the operator needs to do

1. **Merge PR #143** ("Fix daily content cron: surface silent IG mirror failure"). It's still draft-open, `mergeable_state: clean`, +59/-8. After deploy, tomorrow's 14:15 UTC tick will write the exact Meta Graph API error string into every stuck row's `error_message` — that will name the exact scope / IG link problem.
2. **Re-authorize the Meta integration**: relink IG Business Account `17841442248205840` to FB Page `momfluence` (`1086292991223812`) in Meta Business Manager, then re-grant `instagram_basic` + `instagram_content_publish` on the system-user token. FB posting still works, so only the IG side of the token is broken.
3. **Backfill**: after the fix ships and the link is restored, reset the 33 stranded rows from `ig_failed` (post-#143 deploy) back to `fb_published` so `listPendingIgMirror` will retry them.

## Why the FB post URL wasn't checked

`WebFetch https://facebook.com/momfluence/posts` returned HTTP 403 (Facebook blocks unauthenticated crawlers). The FB post's existence is confirmed by the Graph API returning `fb_post_id=1086292991223812_122116124714989743` at 14:01:29 UTC — that ID is the ground-truth signal.
