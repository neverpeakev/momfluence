# Daily content cron health — 2026-07-02

**Status: DEGRADED (day 36 of chronic IG outage)**

## Today

- **FB cron (14:00 UTC):** ✅ published `gen-2026-07-02-d3qxy` at 14:01:16 UTC.
  - angle: `playground-bench-diaper-cream-rec`
  - display: *"She named the diaper cream / on the playground bench."*
  - fb_post_id: `1086292991223812_122115806114989743`
- **IG cron (14:15 UTC):** ❌ silent failure — same pattern as every day since 2026-05-28.
  - Edge log at 14:15:08 UTC shows GET 200, zero serverless log lines, no `error_message` written, no `ig_failed` status.

## Chronic outage

- Last successful IG publish: **2026-05-27 14:15:37 UTC** (36 days ago).
- Rows stranded at `status='fb_published'` with `ig_published_at IS NULL`: **31**
  (`earliest_stranded=2026-05-29`, `latest_stranded=2026-07-02`).
- Vercel runtime errors: still zero for `/api/social/ig-mirror` (silent-200 bug not yet shipped).

## Why nothing is being fixed

Two independent problems, both stuck on human action:

1. **Observability fix — not yet merged.** PRs #134, #138, #139, #141, #142, #143 all
   open the exact same code fix (mark rows `ig_failed` in the `fetchPageContext` catch
   branch, `console.error`, return 500). Every one is a draft. **PR #143** from 2026-07-01
   is the latest; merging any of them unblocks diagnosis of the real error message.
2. **Root cause — Meta re-auth required.** `META_MARKETING_API_TOKEN` still works for the
   FB Page publish 15 minutes earlier, so it is not fully expired. The last non-silent
   failures (2026-05-23 → 2026-05-26) recorded Graph API `error_subcode 33`
   ("Object does not exist, cannot be loaded due to missing permissions, or does not
   support this operation"), pointing at either the IG Business Account link
   (`17841442248205840` ↔ FB Page `1086292991223812`) or missing
   `instagram_basic` / `instagram_content_publish` scopes on the system-user token.

## Action

- **No new fix PR opened today.** Opening a seventh duplicate observability PR would
  add noise, not signal. This branch is a log-only entry.
- Notification sent to operator with the concrete unblocker list (see below).

## Unblocker for operator

1. Merge PR #143 (or any earlier one — they are the same fix). This alone will surface
   the real Graph API error on every stranded row on the next 14:15 UTC tick, so the
   next diagnosis is one-shot.
2. Re-verify in Meta Business Manager:
   - Business Settings → Pages → Momfluence (`1086292991223812`) → Instagram accounts
     shows `@momfluence` (`17841442248205840`).
   - System user owning `META_MARKETING_API_TOKEN` still has `instagram_basic`,
     `instagram_content_publish`, `pages_manage_posts`, `business_management`.
3. If scopes were dropped, mint a fresh long-lived system-user token and update the
   `META_MARKETING_API_TOKEN` env var on the `momfluence-platform` Vercel project.
4. Decide backlog policy for the 31 stranded rows: replay (burns ~2 days of IG's
   25-per-24h publish limit) or skip and resume from tomorrow. Recommend skip —
   most posts are >2 weeks old.
