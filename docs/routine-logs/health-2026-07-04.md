# Daily content cron health — 2026-07-04

Status: **partial (FB ok, IG mirror still silently failing — day 38)**

## Today's row
- slug: `gen-2026-07-04-guo4g`
- angle_tag: `pediatrician-waiting-room-teething-gel-rec`
- display: "She named the teething gel / in the waiting room."
- fb_post_id: `1086292991223812_122115995498989743` (fb_published_at 14:01:28 UTC — healthy)
- ig_media_id: `null`, ig_published_at: `null`, error_message: `null`

## IG mirror outage
- Last successful `ig_published` row: **2026-05-27**. 38 consecutive days of silent failure.
- Vercel log 14:15:08 UTC today: `GET /api/social/ig-mirror 200 [info/edge-middleware]` — same
  200-with-empty-body-and-no-DB-write pattern as every prior day.
- Root cause of invisibility (confirmed on prior runs): `fetchPageContext` throws in the pre-loop
  setup catch branch, which returns 200 with a `message` field and never touches any pending row.
  So the row stays `fb_published` with `error_message = null`, the runtime error tracker never
  fires, and the DB gives no diagnostic to the next day's supervisor.
- Root cause of the underlying Graph API failure is not directly visible from the supervisor
  (no error_message written, no serverless error log surfaced). Working hypothesis carried over
  from prior investigations: the IG Business Account link on FB Page `momfluence` needs to be
  re-authorized in Meta Business Manager and `instagram_basic` + `instagram_content_publish`
  scopes re-granted on the system-user token. Cannot confirm without either merging one of the
  observability-fix PRs (so the next run writes the actual Graph error) or manual Meta console
  access.

## Not opening a duplicate PR today
There are already 24 open draft PRs shipping essentially the same observability fix:
#117 #118 #119 #120 #121 #122 #125 #126 #127 #128 #133 #134 #138 #139 #141 #142 #143,
plus health-log escalation PRs #124 #129 #130 #135 #137 #144. Yesterday's supervisor
(2026-07-02 log) already made the call to stop opening duplicates until an operator merges one.
Holding that line today — the fix is queued, and adding a 25th PR would only add noise.

## What actually unblocks this
1. Merge any one of the observability-fix PRs (recommendation: **#143** — most recent, cleanest
   diff, marks pending rows `ig_failed` on setup failure + returns 500 so Vercel error tracker
   picks it up).
2. Wait one cron cycle. The next run will write the real Graph API error to
   `generated_posts.error_message` for the day's row.
3. Read that error message and either re-auth Meta (most likely) or fix whatever it actually
   surfaces.

## Live-post spot-check
`WebFetch https://facebook.com/momfluence/posts` → 403 (Facebook blocks unauthenticated scrapes).
Same limitation as prior runs; well-formed `fb_post_id` in DB is the fallback proof of publish.
