# health 2026-07-30

**Status:** IG mirror silently failing — **day 63**. FB path healthy. No new PR opened (11th duplicate would be noise).

## Today's row

| field | value |
|---|---|
| slug | gen-2026-07-30-xq3n1 |
| angle_tag | school-drop-off-line-umbrella-rec |
| display | "She named the kids' umbrella / in the drop-off line." |
| status | `fb_published` (stuck — should be `ig_published`) |
| fb_post_id | 1086292991223812_122118486302989743 |
| fb_published_at | 2026-07-30 14:01:10 UTC |
| ig_media_id | `null` |
| ig_published_at | `null` |
| error_message | `null` (silent failure, same pattern as prior 62 days) |

FB Graph API returned a real post_id, so the FB side is confirmed live. IG mirror at 14:15 UTC no-op'd silently, exactly like every day since 2026-05-27.

## Blockers (both need Kevin)

1. **Merge PR #174** — `claude/fix-content-cron-2026-07-28`. Makes ig-mirror `markFailed` on preflight/auth failure so tomorrow's audit surfaces the real cause in 24h instead of another day of silence. Draft, `mergeable_state: clean`. This is the cleanest of PRs #166, #167, #170, #171, #173, #174 — all propose the same observability fix, none merged. Recommend closing #166/#167/#170/#171/#173 in favor of #174.
2. **Re-authorize Meta side** — root cause. Options:
   - Re-mint `META_MARKETING_API_TOKEN` with `instagram_content_publish` + `instagram_basic` + `pages_show_list` scopes, OR
   - Re-link IG Business Account `17841442248205840` to FB Page `1086292991223812` in Meta Business Suite.
   62-day timing matches a Meta long-lived-token / IG-link expiration cycle.

## Rows stuck in `fb_published` (need cleanup once IG restored)

3 rows: 2026-07-28, 2026-07-29, 2026-07-30. Earlier backlog (05-28 → 07-27) already marked `ig_failed` by prior sessions. These 3 will either mirror correctly once Meta is fixed OR need to be manually marked `ig_failed` if Meta stays broken past 48h (they'll be stale by then and shouldn't bulk-publish).

## What this session did

- Queried `generated_posts` for today → confirmed fb_published/null pattern.
- Checked PR #174 → still open, draft, clean.
- Wrote this log. Notified Kevin.
- Did **not** open PR #11 with the same fix. Did **not** touch DB rows.
