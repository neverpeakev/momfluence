# Daily content cron health — 2026-08-27

## Status

- **FB path: healthy.** Row `gen-2026-08-27-p9vx0` inserted at 14:00:55 UTC,
  fb_published at 14:01:18 UTC.
  - fb_post_id: `1086292991223812_122121180932989743`
  - angle_tag: `chess-club-tournament-timer-battery-rec`
  - display: "She named the timer batteries / at the chess club tournament."
- **IG path: still silent no-op — day 31 of the same known outage.**
  - status stuck at `fb_published`; ig_media_id NULL; ig_published_at NULL;
    error_message NULL; errored_at NULL.
  - Vercel runtime logs 14:12–14:25 UTC on the current production deployment
    (`dpl_Dqu8fqQ8MnoMvwYVXsfZZ5YavdL6`) return zero lines for
    `/api/social/ig-mirror` — identical to every day since 2026-07-28.
  - Backlog is now ~31 rows, comfortably over IG's 25/24h publish ceiling —
    any bulk drain after re-link will need throttling across ≥2 days.

## FB post spot-check

`WebFetch https://facebook.com/momfluence/posts` failed with
`EGRESS_BLOCKED`: `facebook.com` is not on this remote-execution session's
allowed egress list, so a live public-page verification is not possible from
this routine. The FB Graph publish returned `fb_post_id
1086292991223812_122121180932989743` and `fb_published_at` was recorded on
the row, which are the same success indicators the routine has relied on for
30 days.

## Why no new code PR from this session

The IG-mirror failure surface is already covered by 20 open draft PRs:

- IG silent failure (10 PRs): #177, #181, #187, #191, #193, #195, #197,
  #199, #200, #202
- FB JSON-parse (1 PR): #198
- Prompt-format monoculture (1 PR): #204
- Log-only / doc PRs (8+): #178, #179, #182, #184, #185, #186, #189, #190,
  #192, #194, #201, #203, #205, #206, #207

The IG outage's actual root cause is upstream of code: the FB Page
`1086292991223812` no longer resolves to a linked Instagram Business Account
via the Graph edge, so `fetchPageContext` throws. **No amount of code will
publish to an IG account that isn't linked at Meta.** Adding a 21st
competing repair PR would fragment review further without changing that.

## Action still required from Kevin (unchanged from 30 prior logs)

1. Meta Business Suite → Business Settings → Accounts → Instagram accounts
   → confirm IG business account `17841442248205840` is linked to FB Page
   `1086292991223812`; re-link if not.
2. Confirm `META_MARKETING_API_TOKEN` still carries `instagram_basic`,
   `instagram_content_publish`, and `pages_show_list`.
3. Decide whether to drain the ~31-row `fb_published`-limbo backlog after
   re-link (throttled across days due to IG's 25/24h ceiling) or write it
   off by marking the older rows `ig_failed`.
4. Pick one of the open IG-mirror PRs to merge for future visibility
   (prior sessions have recommended #202 as the cleanest). The rest can
   then be closed.

## Recommended merge order once IG is re-linked

1. Merge one IG-mirror visibility PR (e.g. #202) so any future outage
   surfaces as `ig_failed` with `error_message` instead of silent 200.
2. Manually POST `/api/social/ig-mirror` with `Bearer $CRON_SECRET` to
   drain the freshest ~10 rows; wait 24h; repeat.
3. Close the remaining 9 competing IG-mirror PRs.
