# Daily content cron health — 2026-08-08

Status: **degraded (FB ok, IG mirror still dark since 2026-05-28)**

## Today's row
- slug: `gen-2026-08-08-3i0a6`
- angle_tag: `orchestra-concert-rosin-rec`
- display: "She named the rosin / at the orchestra concert."
- status: `fb_published` (fb_post_id `1086292991223812_122119489370989743`, fb_published_at 14:01:08 UTC)
- IG: **not mirrored** (ig_media_id null, no error_message written)

## What the cron did
`/api/social/ig-mirror` fired on schedule (14:16:03 UTC, edge access log shows GET → 200) but no serverless output and no DB writes. The endpoint returned 200 silently — one of the early-return paths (missing env vars, `nothing to mirror`, or the `auth/page-context: …` catch) took the request. Because the route only writes `error_message` inside the per-row publish loop, none of those early-return paths leave any DB trace.

## Root cause (already known, unresolved)
The IG Business Account was unlinked from FB Page `1086292991223812` around 2026-05-28. A prior routine run on 2026-07-29 07:24:38 UTC bulk-marked every stuck row (from that period through 2026-07-27) as `ig_failed` with the note: "skipped-stale-backlog: IG business account was unlinked from the FB Page ~2026-05-28 …". Since that cleanup, a new backlog has accumulated: **11 posts from 2026-07-28 through 2026-08-08 are stuck at `status=fb_published, ig_media_id=null`**, and each day's ig-mirror run silently returns 200 without writing an error.

DB roll-up:
- Total FB-published rows ever: 78
- Ever IG-published: 11 (last successful IG at 2026-05-27 14:15:37 UTC)
- ig_failed: 55 (all with the 2026-07-29 backlog-skip marker)
- Currently stuck (fb_published, no ig, no error): 11 (2026-07-28 → 2026-08-08)

## Live-post spot-check
`https://facebook.com/momfluence/posts` blocks unauthenticated fetches (same as 2026-05-13 log). Relying on `fb_post_id` shape as proof of publish. IG side has no post to check — mirror never ran.

## What the user needs to do
Reconnect the IG Business Account (`17841442248205840`) to FB Page `1086292991223812` in Meta Business Suite → Settings → Linked Accounts → Instagram, and grant `instagram_content_publish` on the system-user token backing `META_MARKETING_API_TOKEN`. Once the `/{pageId}?fields=instagram_business_account` call resolves, the mirror will start picking up new posts. **Before restoring the link**, decide what to do with the current 11-row stuck backlog — otherwise the next cron tick will bulk-publish 10 stale posts to IG in one shot (same failure mode the 2026-07-29 cleanup guarded against).

## Observability gap
The ig-mirror route silently returns 200 on `auth/page-context` failure with no `console.error`, so Vercel runtime logs show the invocation as healthy. That's why this has been dark for 2.5 months. Not fixed in this run — flagged for a follow-up code change (add `console.error` on each early-return path and consider writing a route-level "cron_events" row so silent skips are visible in Supabase, not just at the row level).

## PR
None opened this run — root cause is an external Meta config, not a code bug. Notification sent to Kevin.
