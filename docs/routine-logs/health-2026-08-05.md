# Daily content cron health — 2026-08-05

## Today's row

- slug: `gen-2026-08-05-v08k8`
- angle_tag: `carpool-line-window-visor-rec`
- display: "She named the sun visor / in the carpool line."
- FB post_id: `1086292991223812_122119149878989743` (published 14:02:08 UTC)
- IG media_id: — (not mirrored)
- status: `fb_published` (stalled — same failure mode as every day since 2026-07-28)

## Diagnosis

The IG mirror cron ran at 14:15:08 UTC and returned 200, but did not update
any row. Confirmed root cause is the same one previously documented on
2026-07-29: the Instagram Business Account was unlinked from the FB Page
around 2026-05-28. `app/api/social/ig-mirror/route.ts:178-192` catches the
`fetchPageContext` throw and returns 200 with `message: "auth/page-context:
..."` **without** calling `markFailed`, so the row silently stays in
`fb_published`. Nine rows have accumulated in `fb_published` state since
2026-07-28; last successful IG publish was 2026-05-27.

Not a code bug in the sense of "regression this cron introduced" — the
mirror code is behaving as written. The blocker is that the FB Page has
no linked Instagram Business Account. No PR will fix this until Kevin
reconnects the IG account in Meta Business Suite:

  Meta Business Suite → Settings → Accounts → Instagram accounts →
  Connect Instagram account → link IG business acct 17841442248205840
  to FB Page 1086292991223812, and re-authorize the system-user token
  with `instagram_basic` + `instagram_content_publish` scopes.

Once relinked, the mirror will resume automatically. The 9 backlogged
`fb_published` rows would then bulk-publish on the next cron tick — Kevin
may want to first mark them `ig_failed` (like the prior 2026-07-29
cleanup) to avoid dumping 9 old posts to IG in one day.

## Actions

- No PR opened — infrastructure issue, not code.
- Push notification sent to Kevin.
