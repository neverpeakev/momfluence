# Optimizer Tick — 2026-08-06 00:00 UTC

- **Tick ID:** `30c36673-ad1c-4660-9872-586d6c14930f`
- **occurred_at:** 2026-08-06 00:00:58 UTC
- **Mode:** `shadow`
- **duration_ms:** 2248
- **variants_seen:** 10
- **ads_seen:** 45
- **signups_seen:** 2
- **data_pull_ok:** true

## Decision counts

| action_type | n |
|---|---|
| no_op | 10 |

## Status

Awaiting data — 0 visits across all 10 variants in this tick's window, posteriors still flat (p_best ≈ 0.10 for every variant, i.e. uniform prior). Every variant tripped the same guard: `visits=0 (need ≥100), conv=0 (need ≥5)`.

Note: `signups_seen=2` at the tick level but per-variant `visits`/`conversions` are all zero — likely the signups can't yet be attributed to a variant (fresh window, attribution lag, or signups from a non-ad source). Worth watching over the next 1–2 ticks; if signups keep appearing globally but never at the variant level, the attribution join is broken.

No shadow decisions to review — the optimizer would have done nothing in live mode this tick.
