# Daily content cron health — 2026-07-19

Status: **degraded — IG mirror silently broken since 2026-05-29 (44-row backlog)**

FB half of today's run is healthy. IG half has been silently failing for ~52 consecutive days without any log line or DB failure marker — the health check exists precisely to surface exactly this class of drift.

## Today's row (FB only)
- slug: `gen-2026-07-19-j1n9s`
- angle_tag: `airport-gate-toddler-headphones-rec`
- display: "She named the headphones / at the airport gate."
- fb_post_id: `1086292991223812_122117521664989743` (fb_published_at 14:01:59 UTC)
- ig_media_id: **null** — status stuck at `fb_published`

## Backlog
`SELECT count(*) FROM generated_posts WHERE status='fb_published' AND ig_published_at IS NULL` → **44 rows**
- oldest: `gen-2026-05-29-…` (fb_published 2026-05-29 14:01)
- newest: today
- Last successful `ig_published`: `gen-2026-05-27-p8u57`, 2026-05-27 14:15:37 UTC

The break lines up with a pattern shift in `generated_posts`:
- Through 2026-05-26 failures wrote `ig_failed` rows (3 total, all `IG status check → 400 GraphMethodException code=100 subcode=33` — per-container status polling errors caught inside the per-row loop).
- From 2026-05-29 onward every failure is silent — no `ig_failed` row, no `error_message`, and Vercel runtime logs show only `GET /api/social/ig-mirror 200` with no function-side log line.

## Root cause (code side)
`app/api/social/ig-mirror/route.ts` catches `fetchPageContext` errors and returns 200 with `{ message: "auth/page-context: …" }`, but **never calls `console.error`**. Vercel doesn't surface response bodies in the runtime-log view, so the failure is completely invisible: no log, no DB row change, and the endpoint keeps advertising 200. Meanwhile the same 44 rows re-appear in `listPendingIgMirror(10)` every day, never advance, and never get marked failed.

## Root cause (infra side — unverified, needs Kevin)
`fetchPageContext` fails at either `/me/accounts` (page token) or `/{pageId}?fields=instagram_business_account` (IG lookup). Since `fb-daily-generate` succeeded today using the same `META_MARKETING_API_TOKEN` + page id and the same `/me/accounts` call, the page token itself is valid — which points at the **IG lookup**:
- The linked IG Business Account (`17841442248205840`) was disconnected from FB Page `1086292991223812`, **or**
- The token lost the `instagram_basic` / `instagram_content_publish` scope on ~2026-05-28.

Cannot verify from this session (no FB token access). Once the code fix below is deployed, tomorrow's 14:15 UTC run will log the exact Graph-API error message to Vercel logs, which will disambiguate the two.

## Fix
PR: (see branch `claude/fix-content-cron-2026-07-19`)
1. `app/api/social/ig-mirror/route.ts` — add `console.error` on `fetchPageContext` failure with the Graph-API error text and the pending-row count so the failure shows up in Vercel runtime logs immediately. Add a `console.log` on the healthy path noting how many rows are being processed. Do **not** mark the 44 stuck rows `ig_failed` — leave them `fb_published` so they mirror automatically once IG link/scope is restored.

## Follow-up for Kevin (not automatable)
1. Open Meta Business Suite → Momfluence FB Page → Instagram accounts → confirm `@momfluence` IG Business Account is still linked; re-link if not.
2. In Meta Business Settings → System Users → the user backing `META_MARKETING_API_TOKEN`, confirm the token still has `instagram_basic` and `instagram_content_publish` permissions on the Momfluence FB Page + IG account.
3. After re-linking / re-granting, manually POST `/api/social/ig-mirror` with the CRON_SECRET bearer. It will chew through the 44-row backlog (capped at 10 per invocation — needs to be fired 5×), or bump the `listPendingIgMirror(10)` cap for a one-time drain.

## Live-post spot-check
WebFetch of `facebook.com/momfluence/posts` returned 403 (FB blocks unauthenticated scrapes, same as prior log). Trusting `fb_post_id=1086292991223812_122117521664989743` from the DB row as proof of FB publish; no IG-side proof (nothing to verify).
