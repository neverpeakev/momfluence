# Daily content-cron health — 2026-08-23

**Status:** FB healthy, IG mirror silently no-op (same known failure).

## Today's row

- `id`: `ab434f25-b24b-40a0-89dc-c783c1b208ed`
- `slug`: `gen-2026-08-23-tfmn6`
- `angle_tag`: `art-class-smock-parent-volunteer-rec`
- `display`: "She named the art smock / at the parent volunteer table."
- `status`: `fb_published`
- `fb_post_id`: `1086292991223812_122120847440989743`
- `fb_published_at`: 2026-08-23 14:01:16 UTC
- `ig_media_id`: NULL
- `ig_published_at`: NULL
- `error_message`: NULL, `errored_at`: NULL

FB post ID assigned by Meta — FB path is healthy end-to-end. FB post URL not
verified via WebFetch (facebook.com blocked by egress proxy this session).

## IG mirror

Same failure mode as every day since ~2026-05-28. IG cron scheduled 14:15 UTC,
health check ran at 14:45 UTC (30 min after) — row still `fb_published`, no
`error_message`. `fetchPageContext` in `app/api/social/ig-mirror/route.ts:58-78`
throws because the IG Business Account (`17841442248205840`) is unlinked from
FB Page (`1086292991223812`), and the endpoint returns via the
`auth/page-context` early-exit at `app/api/social/ig-mirror/route.ts:182-192`
without marking any row `ig_failed`. Vercel counts the run as HTTP 200.

## Not opening a new fix PR

Three prior Claude PRs already diagnose this identically and none are merged:

- #200 (2026-08-20) — draft; code fix + `META_IG_USER_ID` env-var proposal.
- #201 (2026-08-21) — draft; log-only.
- #202 (2026-08-22) — draft; code fix (mark rows `ig_failed` on page-context throw + logging).

This PR is log-only. No new code diagnosis to add. The unblocker is a Meta
Business Suite action Kevin has to take by hand (relink `@momfluence` IG
business account to FB Page `facebook.com/momfluence`); then either merge #200
or #202 for the visibility fix, and decide what to do with the growing
`fb_published`-limbo backlog.

## Backlog

Row 25 of the same limbo stack (2026-07-28 → 2026-08-23). Now well over IG's
25-publishes / 24h rolling ceiling — a re-linked account will not be able to
drain them all in one day; either bulk-mark them `ig_failed` or accept a
multi-day drain when IG comes back online.
