# Daily content cron health — 2026-05-21

Status: **blocked — MCP access not granted**

Could not complete the routine verification because the required MCP tool calls returned `MCP tool call requires approval` (twice each) and never resolved:

- `mcp__Supabase__execute_sql` against project `gndzxfrqfpszqocbsktn` — needed to read today's row from `public.generated_posts`.
- `mcp__Vercel__list_teams` (and downstream `get_runtime_logs` / `list_deployments`) — needed to inspect cron invocations for `/api/social/fb-daily-generate` (14:00 UTC) and `/api/social/ig-mirror` (14:15 UTC) on the `momfluence-platform` project.

Per session rules I did not retry beyond the second attempt. To re-run this check, approve the Supabase + Vercel MCP tools (or pre-allow them in settings) and re-trigger the routine.

## What I was able to verify
- Repo cloned clean, HEAD `5b19c3f` on `main`.
- `vercel.json` confirms the two daily crons are still scheduled at `0 14 * * *` and `15 14 * * *`.
- `WebFetch https://facebook.com/momfluence/posts` → HTTP 403 (Facebook blocks unauthenticated scrapes, same as the 2026-05-13 run). Live-post spot-check requires Graph API access, not WebFetch.

## Next steps for re-run
1. Approve Supabase MCP and re-run the `SELECT … FROM public.generated_posts WHERE created_at >= date_trunc('day', now())` query.
2. Approve Vercel MCP so `get_runtime_logs` can be filtered to `/api/social/fb-daily-generate` and `/api/social/ig-mirror` around 14:00–14:30 UTC on 2026-05-21.
3. If today's row is missing or in `fb_failed`/`ig_failed`, branch from `main` as `claude/fix-content-cron-2026-05-21` and open a PR.

No code changes pushed; no PR opened.
