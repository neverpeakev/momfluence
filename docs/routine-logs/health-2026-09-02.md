# Daily content-cron health — 2026-09-02

**Status:** FB healthy, IG mirror silent (day 36 of same outage).

## Today's row (public.generated_posts)
- id: `af790839-9ff1-4c12-8c4c-dd7fca397efa`
- slug: `gen-2026-09-02-73tm1`
- angle_tag: `soccer-practice-sideline-shin-guard-tape-rec`
- display: "She named the shin guard tape / on the soccer sideline."
- status: `fb_published` (not `ig_published`)
- fb_post_id: `1086292991223812_122121696650989743`
- fb_published_at: `2026-09-02 14:01:24 UTC`
- ig_media_id: `null`
- ig_published_at: `null`
- error_message: `null`, errored_at: `null`

## Diagnosis
Same silent no-op that has stranded every daily FB post since 2026-05-27:
the ig-mirror endpoint's `fetchPageContext` catch swallows the error and
returns `200 { ok: false }` with no console.error, no `error_message`
written to the row. External root cause: the IG Business Account
(`17841442248205840`) is no longer linked to FB Page `momfluence`
(`1086292991223812`) in Meta Business Suite, so Graph's
`/{page-id}?fields=instagram_business_account` returns nothing and the
mirror never publishes.

## No new code PR
Not opening a fresh code-fix PR today. The repair surface is already
covered by 40+ open draft PRs (#166, #167, #170, #171, #173, #174, #177,
#181, #187, #191, #193, #195, #197, #200, #202, #209, #213, …) — piling
on another one is noise. Prior sessions have converged on the same
conclusion: the fix is **Kevin re-linking the IG account in Meta
Business Suite**, then any one of the merged-only fix PRs surfaces
future failures loudly.

## Spot-check
`WebFetch https://facebook.com/momfluence/posts` blocked by the egress
proxy (facebook.com not on the allowlist). Confidence in FB publish
comes from the Graph API returning a fb_post_id and stamping
`fb_published_at` on the row.

## Action needed from Kevin
1. Meta Business Suite → Momfluence Page → **Linked accounts** → re-link
   Instagram Business Account `17841442248205840`.
2. Merge one of the surfacing-observability PRs (#213 is the freshest)
   so future outages produce visible log lines instead of silent 200s.
3. Backfill or discard the 36 stranded `fb_published` rows if IG mirror
   is meant to catch them up.
