# Optimizer tick — 2026-07-07 06:00 UTC

**Tick ID:** `2198d858-4262-4eb2-84b1-ec3711354770`
**Mode:** `shadow`
**Duration:** 1699 ms
**Variants seen:** 10 · **Ads seen:** 45 · **Signups seen:** 2
**Data pull OK:** ✅

## Decision summary

| decision | count |
| --- | --- |
| NO-OP | 10 |
| PAUSE | 0 |
| SCALE | 0 |
| REMIX | 0 |

Awaiting data — every arm reported `visits=0`, `conversions=0`. Posterior is still the uniform prior (all `p_best ≈ 0.10`), so the optimizer correctly held with 10× `no_op` under the "insufficient data" rule (min_visits_per_arm=100, min_conversions_per_arm=5).

## Notes

- Tick-level counters (`ads_seen=45`, `signups_seen=2`) are non-zero but arm-level `visits`/`conversions` are all zero. This is expected in early observation: signups exist at the tick aggregate level but haven't yet been attributed to any variant arm (attribution lag or events fired outside the variant funnel). Nothing to action; will re-check at the 12:00 tick.
- No shadow decisions to translate — nothing would have fired in live mode either.

## Shadow decisions for review

None this tick.
