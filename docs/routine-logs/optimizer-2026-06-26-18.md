# Optimizer tick — 2026-06-26 18:00 UTC

## Tick

- **id:** `d9326ba8-c200-4786-ba8d-a0b19f9559c4`
- **occurred_at:** 2026-06-26 18:00:09 UTC
- **mode:** `shadow`
- **duration_ms:** 2261
- **variants_seen:** 10
- **ads_seen:** 45
- **signups_seen:** 2
- **data_pull_ok:** true

## Decision counts

| action_type | count |
|---|---|
| no_op | 10 |

## Summary

Awaiting data — **0 visits and 0 conversions across all 10 variants** in this tick window; the posterior is still flat (p_best clustered tightly between 0.0957 and 0.1070, i.e. uniform-ish over 10 arms ≈ 0.10 each). Every arm tripped the insufficient-data gate (`min_visits_per_arm=100`, `min_conversions_per_arm=5`).

Note: the tick header reports `signups_seen=2` and `ads_seen=45` but the per-action visit/conversion counts are all zero — likely the per-arm attribution window or join hasn't picked them up yet. Worth eyeballing in tomorrow's tick if it persists, but not actionable right now.

## Shadow decisions for review

None — no PAUSE/SCALE/REMIX would have fired in live mode this tick. The optimizer is correctly holding back until per-arm traffic clears the gates.

## Settings snapshot

- mode: shadow
- min_visits_per_arm: 100, min_conversions_per_arm: 5
- p_best_threshold: 0.80, p_worst_threshold: 0.95
- max_pauses_per_tick: 1, max_scales_per_tick: 1
- scale_pct_per_day: 0.30, max_budget_multiplier: 3.0
- starting_daily_budget: $30
