# Optimizer tick — 2026-08-05 06:00 UTC

- **Tick ID:** `9dba9838-e6ee-4137-8c49-2582cb0713c0`
- **Occurred at:** 2026-08-05 06:01:05 UTC
- **Mode:** `shadow`
- **Duration:** 2,848 ms
- **Variants seen:** 10
- **Ads seen:** 45
- **Signups seen (tick-level):** 2
- **Data pull OK:** true
- **Error:** none

## Decision counts

| type   | count |
|--------|-------|
| no_op  | 10    |
| pause  | 0     |
| scale  | 0     |
| remix  | 0     |

## Status: awaiting data

All 10 variants returned `no_op` with `visits=0, conversions=0` at the per-variant level. Rationale on every action: _"Insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)"_. Posteriors are still on the uniform prior (`p_best` and `p_worst` clustered ~0.096–0.107, i.e. ~1/10 for 10 arms).

Expected on day 1–3 of the shadow observation window — no decisions to preview.

## Anomaly worth flagging

Tick-level counters recorded `signups_seen=2` and `ads_seen=45`, but every per-variant row shows `visits=0` and `conversions=0`. The 2 signups did not attribute to any of the 10 tracked variant slugs. Likely explanations, in order of probability:

1. **UTM/landing-page tracking gap** — signups came through a channel that isn't tagging the `variant` param the optimizer keys off.
2. **Attribution window mismatch** — the tick aggregator counted signups in a different window than the per-variant visits query.
3. **Meta ad delivery not started for these ads yet** (ads_seen=45 counts creatives fetched from Meta, not creatives that served impressions).

Not urgent while in shadow mode, but if the same pattern holds after 2–3 more ticks (i.e. signups > 0 but per-variant visits stay at 0 across the board), the optimizer will never accumulate the ≥100 visits / ≥5 conversions per arm it needs to move off `no_op`, and the shadow window will produce no meaningful decisions to review. Worth checking the visit-attribution pipeline before end of week 1.

## Shadow decisions for review

None — no non-no_op decisions this tick.

---

Settings snapshot (unchanged from 2026-05-11):
`mode=shadow`, `max_pauses_per_tick=1`, `max_scales_per_tick=1`, `min_visits_per_arm=100`, `min_conversions_per_arm=5`, `p_worst_threshold=0.95`.
