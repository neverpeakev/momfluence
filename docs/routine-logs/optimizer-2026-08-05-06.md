# Optimizer tick — 2026-08-05 06:00 UTC

- **Tick ID:** `9dba9838-e6ee-4137-8c49-2582cb0713c0`
- **Occurred at:** 2026-08-05 06:01:05 UTC
- **Mode:** `shadow`
- **Duration:** 2,848 ms
- **Variants seen:** 10
- **Ads seen:** 45
- **Signups seen (tick-level):** 2
- **Data pull OK:** true
- **Error:** none

## Decision counts

| type   | count |
|--------|-------|
| no_op  | 10    |
| pause  | 0     |
| scale  | 0     |
| remix  | 0     |

## Status: awaiting data

All 10 variants returned `no_op` with `visits=0, conversions=0` at the per-variant level. Rationale on every action: _"Insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)"_. Posteriors are still on the uniform prior (`p_best` and `p_worst` clustered ~0.096–0.107, i.e. ~1/10 for 10 arms).

Expected on day 1–3 of the shadow observation window — no decisions to preview.

## Anomaly worth flagging — attribution pipeline appears dead

This is not a first-tick oddity. Checked the last 14 days of history:

- **56 ticks in the last 14 days**, all `data_pull_ok=true`, all running on the 6h schedule.
- **560 actions written** (56 ticks × 10 arms). `SUM(visits)=0`, `SUM(conversions)=0`, `MAX(visits per arm)=0`. Every single row.
- **Every tick reports the exact same `signups_seen=2`** for at least the last 10 ticks (~60 hours). It is not accumulating — it looks like a stuck lifetime count of the same 2 legacy signups, not new activity.

So the optimizer machinery is healthy but flying blind. In shadow mode this is invisible (nothing to enact, no alarms). In live mode it would be equally useless — with no per-variant visits, `no_op` is the only decision the algorithm can ever emit, and there is nothing to promote out of shadow.

Likely causes, in order of probability:

1. **Per-variant visit query is broken or pointed at the wrong table/column** — most likely the query the optimizer runs to build the per-arm rows filters on a `variant` column that stopped being populated (e.g. after the funnel rebuild in #99 / embedded checkout in #105/#108, the landing page may no longer be stamping variant on session rows).
2. **UTM parameter dropped in the funnel rewrite** — the ads presumably link to `?variant=<slug>`, but the new funnel routes/redirects may be stripping it before it hits the analytics insert.
3. **Aggregator's `signups_seen` is a hard-coded or cached lifetime read** — the fact that it's been exactly `2` for 60 hours across independent tick invocations is suspicious on its own, regardless of variant attribution.

Recommendation: before the next shadow → live flip, verify (a) that new visitors from Meta ads are producing rows the optimizer's per-variant query can see, and (b) what `signups_seen=2` is actually counting. The three-month-old mode setting (`updated_at 2026-05-11`) suggests this has been running like this the whole time.

## Shadow decisions for review

None — no non-no_op decisions this tick.

---

Settings snapshot (unchanged from 2026-05-11):
`mode=shadow`, `max_pauses_per_tick=1`, `max_scales_per_tick=1`, `min_visits_per_arm=100`, `min_conversions_per_arm=5`, `p_worst_threshold=0.95`.
