# Daily content cron health — 2026-08-02

Status: **fb ok / ig silent-fail (day 66 of chronic outage, day 6 of new backlog)**

## Today's row
- slug: `gen-2026-08-02-p97gb`
- angle_tag: `parent-teacher-conference-hallway-tissue-rec`
- display: "She named the tissues / in the conference hallway."
- FB: `fb_post_id 1086292991223812_122118821066989743` at 14:01:25 UTC ✅
- IG: `status='fb_published'`, `ig_media_id=null`, `error_message=null` ❌ (same silent pattern as every day since 2026-05-29)

## Same as yesterday
No new signal. `ig-mirror` cron fired at 14:15:08 UTC and returned HTTP 200 (edge-middleware log only, no serverless output), consistent with `fetchPageContext` throwing on the `/{pageId}?fields=instagram_business_account` lookup and being caught by the pre-flight `try/catch` that doesn't call `markFailed`. Root cause is unchanged: Instagram Business Account `17841442248205840` is not linked to FB Page `1086292991223812`, and `META_MARKETING_API_TOKEN` may also be missing `instagram_basic` / `instagram_content_publish` scopes.

## Backlog piling up
Six FB-published rows now sit at `status='fb_published'`, `ig_media_id=null`, `error_message=null` — exactly one week's worth since the 2026-07-29 manual backfill that marked the older backlog `ig_failed / skipped-stale-backlog`:

| slug | fb_published_at |
| --- | --- |
| `gen-2026-07-28-mzqmt` | 2026-07-28 14:02:04 |
| `gen-2026-07-29-59bnh` | 2026-07-29 14:01:10 |
| `gen-2026-07-30-xq3n1` | 2026-07-30 14:01:10 |
| `gen-2026-07-31-aeqj7` | 2026-07-31 14:02:08 |
| `gen-2026-08-01-5srdd` | 2026-08-01 14:01:10 |
| `gen-2026-08-02-p97gb` | 2026-08-02 14:01:25 |

If the IG link is restored without another manual `ig_failed` sweep first, `ig-mirror` will bulk-publish all six to IG on its next run (up to its `limit=10` batch cap).

## No new fix PR opened
Ten-plus `claude/fix-content-cron-*` PRs are already open and unmerged, all proposing the same one-file fix: call `markFailed` on pending rows when the pre-flight throws. Any one of them, merged, would give tomorrow's audit a real `error_message` to reason about. **PR #174 has the cleanest diff and clearest write-up** — that's the one to merge. Opening an 11th duplicate fix PR would just add noise.

Production is still on `8a63851` (2026-06-02 `main` HEAD). No IG fix has ever shipped.

## Owner action (unchanged from prior daily logs)
1. Merge PR #174 (or any of #170 / #171 / #173 / #177) so future outages surface the real Meta error in `error_message` within 24h instead of silently piling up.
2. Re-link Instagram Business Account `17841442248205840` to FB Page `1086292991223812` in Meta Business Suite → Business Assets → Instagram accounts, **or** re-mint `META_MARKETING_API_TOKEN` with scopes `instagram_basic`, `instagram_content_publish`, `pages_show_list`, `pages_manage_posts`, `pages_read_engagement`, `business_management` and update in Vercel env.
3. Before re-linking, decide the backlog handling:
   - Bulk publish today: leave the six stuck rows alone.
   - Skip the backlog: `UPDATE generated_posts SET status='ig_failed', errored_at=now(), error_message='skipped-stale-backlog: manual backfill 2026-08-02' WHERE status='fb_published' AND ig_media_id IS NULL AND fb_published_at < '2026-08-02';` (keeps only today's row eligible).

## Live-post spot-check
Skipped WebFetch — `facebook.com/momfluence/posts` returns 403 for unauthenticated fetches (historical note from 2026-05-13 log). Well-formed `fb_post_id` (`<page_id>_<numeric>`) is proof of publish path.
