# Daily content cron health — 2026-06-03

**Status:** partial — FB published, IG silently failed (day **4** of recurrence).

## Today's run

| Cron | Vercel result | DB result |
|---|---|---|
| `/api/social/fb-daily-generate` (14:00 UTC) | 200 at 14:00:42 | `gen-2026-06-03-11y09` → `status=fb_published`, `fb_post_id=1086292991223812_122112971228989743`, `fb_published_at=14:01:17 UTC` |
| `/api/social/ig-mirror` (14:15 UTC) | 200 at 14:15:08, no `console.error`, no log payload | row untouched: `ig_published_at=NULL`, `ig_media_id=NULL`, `error_message=NULL`, `errored_at=NULL` |

FB row: `angle_tag=sephora-checkout-mascara-rec`, display "She told the mom in line / about her mascara." The FB post id was returned by the Graph API and persisted, so the photo was accepted by FB. (WebFetch of `facebook.com/momfluence/posts` returns 403 from this environment, so I can't visually confirm the post is live — but the Graph API ack is reliable evidence.)

## Backlog of stuck rows (status=fb_published, ig_published_at NULL, error_message NULL)

Four rows, all in the same silent shape:

- `gen-2026-06-03-11y09` (today)
- `gen-2026-06-02-btc5f`
- `gen-2026-05-31-zimk2`
- `gen-2026-05-29-kvrzn`

The last row that captured a real Meta error was `gen-2026-05-26-1jy5d` on 2026-05-26, with `error_message`:

```
IG status check → 400: {"error":{"message":"Authorization Error","code":100,
"type":"GraphMethodException","error_subcode":33,...}}
```

Subcode 33 / GraphMethodException is consistent with the IG Business Account no longer being reachable from the page token (either unlinked from the FB page or `instagram_basic` / `instagram_content_publish` scope dropped on the system-user token).

## Why today's failure is silent

Same diagnosis as 2026-05-31, 2026-06-02 (see PR #101). Between 2026-05-26 (last visible error) and 2026-05-29 (first silent day), the failure point moved from inside the per-row publish loop (where `markFailed` catches the error) to the pre-loop `fetchPageContext` call, whose `catch` block returns 200 with `{ok:false, message:...}` but writes nothing to the DB and emits no `console.error`. Net effect: the cron looks healthy to Vercel and to the DB.

The env-var early-exit is ruled out — `fb-daily-generate` on the same deployment (`dpl_Gvbxxuy1j3CFJGHXUTDCRSWMouVS`, commit `8a63851`) reads the same `META_MARKETING_API_TOKEN` and `META_FB_PAGE_ID` and worked today.

## Action

No new PR opened — the code-side fix is already on PR #101 (`claude/fix-content-cron-2026-05-31`, draft, day 4 open):
<https://github.com/neverpeakev/momfluence/pull/101>

That PR replaces the silent pre-loop early-exits with `abortPending(reason)`, which `console.error`s and `markFailed`s every row in `listPendingIgMirror`. Once merged, the next run will write the real Meta error into `error_message` and we can act on it.

Posted a day-4 nudge comment on PR #101 with today's stuck-row count.

## Required manual follow-up (still pending from 2026-05-31)

1. Merge PR #101 (after rebase against current `main`, `8a63851`).
2. In Meta Business Suite, confirm Instagram Business Account `17841442248205840` is still linked to FB Page `1086292991223812`, and that the system-user token used for `META_MARKETING_API_TOKEN` still holds `instagram_basic` + `instagram_content_publish`.
3. After both, manually kick `/api/social/ig-mirror` so the four stuck rows surface their real Meta error string (post-PR-#101) or actually publish (post-Meta-fix).
4. If publish succeeds for the older rows, IG captions/images may be stale; consider resetting them instead:
   ```sql
   UPDATE public.generated_posts
   SET status = 'ig_failed',
       error_message = 'manually skipped — too stale to mirror',
       errored_at = now()
   WHERE slug IN ('gen-2026-05-29-kvrzn','gen-2026-05-31-zimk2',
                  'gen-2026-06-02-btc5f','gen-2026-06-03-11y09')
     AND ig_published_at IS NULL;
   ```
