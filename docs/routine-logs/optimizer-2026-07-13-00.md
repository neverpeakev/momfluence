# Optimizer Tick — 2026-07-13 00:00 UTC

## Tick metadata

| Field | Value |
|---|---|
| Tick ID | `ed0ff7bb-b8c5-49dd-a9f1-a373cafa374c` |
| Occurred at | 2026-07-13 00:00:09 UTC |
| Mode | `shadow` |
| Duration | 2,095 ms |
| Variants seen | 10 |
| Ads seen | 45 |
| Signups seen | 2 |
| `data_pull_ok` | true |
| Error | — |

## Decision counts

| Action | Count |
|---|---|
| NO-OP | 10 |
| PAUSE | 0 |
| SCALE | 0 |
| REMIX | 0 |

## Summary

Awaiting data — **0 visits across 10 variants** in the per-variant rollup, posterior still flat (p_best clustered ~0.096–0.107, essentially uninformative). Every variant tripped the same guard: `Insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)`.

No shadow decisions to translate for this tick — the optimizer had nothing to act on.

## Note for Kevin

The tick header shows `ads_seen=45` and `signups_seen=2`, but every per-variant action row records `visits=0` / `conversions=0`. Two harmless explanations are possible:

1. The aggregate counters read a different table/window than the per-variant rollup, and the per-variant table hasn't been backfilled yet (day-1 of observation).
2. Attribution joins signups to variants via a key that hasn't propagated yet.

Not blocking — the optimizer is correctly holding fire either way — but worth a glance next tick to confirm per-variant visits start populating. If we're still at visits=0 across the board 24h from now while `ads_seen` keeps climbing, the ingest join is likely broken.
