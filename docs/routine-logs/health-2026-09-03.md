# Daily content-cron health — 2026-09-03

**Status:** FB healthy, IG mirror silent (day 37 of same outage).

## Today's row (public.generated_posts)
- id: `8974d932-7c2a-4484-b88f-11a5ab3ab15d`
- slug: `gen-2026-09-03-1ms4u`
- angle_tag: `dance-recital-backstage-bobby-pins-rec`
- display: "She named the bobby pins / backstage at the dance recital."
- status: `fb_published` (not `ig_published`)
- fb_post_id: `1086292991223812_122121782474989743`
- fb_published_at: `2026-09-03 14:01:59 UTC`
- ig_media_id: `null`
- ig_published_at: `null`
- error_message: `null`, errored_at: `null`

## Diagnosis
Same silent no-op that has stranded every daily FB post since 2026-05-27:
the ig-mirror endpoint's `fetchPageContext` catch swallows the error and
returns `200 { ok: false }` with no console.error, no `error_message`
written to the row. Vercel runtime log confirms the cron fired on
schedule — `GET /api/social/ig-mirror 200` at 14:16:03 UTC on
`dpl_Gvbxxuy1j3CFJGHXUTDCRSWMouVS` — and produced zero application
log lines, exactly as the prior 36 days.

External root cause remains: the IG Business Account
(`17841442248205840`) is no longer linked to FB Page `momfluence`
(`1086292991223812`) in Meta Business Suite, so Graph's
`/{page-id}?fields=instagram_business_account` returns nothing and the
mirror never publishes.

## No new code PR
Continuing the log-only convention from prior sessions. The repair
surface is already covered by 40+ open draft PRs (#166, #167, #170,
#171, #173, #174, #177, #181, #187, #191, #193, #195, #197, #200, #202,
#209, #212, #213, #214, …). Piling on a 41st that no one will merge is
noise. The fix is external: Kevin re-linking the IG account in Meta
Business Suite, then merging one of the surface-errors PRs (#213 is the
freshest that adds `console.error` to every non-success return path in
ig-mirror).

## Spot-check
`WebFetch https://facebook.com/momfluence/posts` blocked by the egress
proxy (facebook.com not on the allowlist). Confidence in FB publish
comes from the Graph API returning a `fb_post_id` and stamping
`fb_published_at` on the row.

## Action needed from Kevin
1. Meta Business Suite → Momfluence Page → **Linked accounts** → re-link
   Instagram Business Account `17841442248205840`.
2. Merge one of the surface-observability PRs (#213 is the freshest) so
   future outages produce visible log lines instead of silent 200s.
3. Backfill or discard the 37 stranded `fb_published` rows if IG mirror
   is meant to catch them up.
