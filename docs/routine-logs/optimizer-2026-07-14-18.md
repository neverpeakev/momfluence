# Optimizer tick — 2026-07-14 18:00 UTC

- **Tick ID:** `569c0ddc-8eb7-41e5-be0e-759491a6b22a`
- **Occurred at:** 2026-07-14 18:00:59 UTC
- **Mode:** shadow
- **Duration:** 2335 ms
- **Variants seen:** 10
- **Ads seen:** 45
- **Signups seen:** 2
- **Data pull OK:** true

## Decision counts

| Decision | Count |
|----------|-------|
| NO-OP    | 10    |
| PAUSE    | 0     |
| SCALE    | 0     |
| REMIX    | 0     |

All 10 variants gated on the `visits ≥ 100` / `conv ≥ 5` threshold. Posteriors still flat (`p_best` cluster 0.096–0.107).

## Awaiting data — but attribution gap worth watching

Tick-level counters recorded **45 ads** and **2 signups**, yet every per-variant action row shows `visits=0, conversions=0`. Two possibilities:

1. The 2 signups this window came in via non-attributed traffic (organic/direct) and never hit a variant-tagged landing page — expected in shadow week, not alarming yet.
2. The variant attribution join (visits → variant_slug) is dropping rows. If tomorrow's ticks still show `visits=0` across the board while `signups_seen` continues to grow, the tracking pipeline needs a look before the observation window's data is trustworthy.

No action required this tick. Flagging so it's on the radar for the next 06:00 UTC review.

## Shadow decisions for review

None — every variant is NO-OP. In live mode nothing would have been paused, scaled, or remixed this tick.
