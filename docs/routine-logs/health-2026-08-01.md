# Daily content cron health — 2026-08-01

Status: **fb ok / ig blocked — 5th consecutive day**

FB post published successfully at 14:01:10 UTC. IG mirror fired at 14:16:04 UTC and returned 200, but the row was never marked `ig_published` or `ig_failed` — it exited early inside `fetchPageContext` because the Meta Graph API returned no `instagram_business_account` for FB Page `1086292991223812`.

## Today's row
- slug: `gen-2026-08-01-5srdd`
- angle_tag: `dance-recital-backstage-bobby-pins-rec`
- display: "She named the bobby pins / backstage at the recital."
- fb_post_id: `1086292991223812_122118710804989743` (fb_published_at 14:01:10 UTC)
- ig_media_id: null · ig_published_at: null · error_message: null · status: `fb_published`

## Root cause
Not a code bug — the Instagram Business Account is still unlinked from the FB Page. Prior Claude session (2026-07-29) documented this on rows `gen-2026-07-27-…` and `gen-2026-07-26-…` when it marked them `ig_failed` with `skipped-stale-backlog: IG business account was unlinked from the FB Page ~2026-05-28`.

Since then, four more days have accumulated in the same stuck state (fb_published, no IG, no error_message):
- 2026-07-28 `bus-stop-corner-mittens-rec`
- 2026-07-29 `library-storytime-rug-board-book-rec`
- 2026-07-30 `school-drop-off-line-umbrella-rec`
- 2026-07-31 `soccer-sideline-shin-guards-rec`
- 2026-08-01 `dance-recital-backstage-bobby-pins-rec` (today)

The mirror endpoint's failure mode is silent by design — `fetchPageContext` throws, the handler catches it, returns `{ok: false, message: "auth/page-context: No Instagram Business Account linked…"}` with HTTP 200 and never touches the row. Vercel edge logs confirm the endpoint fired (14:16:04) but no `/api/render/post/…` pre-warm was issued, which matches the pre-context-lookup exit.

## Not opening a PR
The fix is operational (re-link the linked Instagram Business Account to the FB Page in Meta Business Suite), not code. A code-side improvement worth considering later: when `fetchPageContext` fails, mark each pending row `ig_failed` with the underlying message so this state is visible in the DB instead of silently skipped. Flagged for follow-up, not fixed today.

## Live-post spot-check
FB WebFetch returns 403 (anonymous FB scrape blocked). Verified via well-formed `fb_post_id` from the cron's own success path.
