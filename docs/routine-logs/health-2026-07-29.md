# Daily content cron health — 2026-07-29

Status: **ok on FB, still broken on IG (day ~62)**

## Today's row
- slug: `gen-2026-07-29-59bnh`
- angle_tag: `library-storytime-rug-board-book-rec`
- display: "She named the board book / on the storytime rug."
- fb_post_id: `1086292991223812_122118395102989743` (fb_published_at 14:01:10 UTC) ✅
- ig_media_id: `null`, ig_published_at: `null`, error_message: `null` — same silent state as every day since 2026-05-28

Vercel log for the 14:15:08 UTC IG cron: `GET /api/social/ig-mirror 200 [info/edge-middleware]`, no serverless output. Consistent with the `fetchPageContext` early-return path (IG Business Account `17841442248205840` no longer resolvable from FB Page `1086292991223812`, or token missing `instagram_basic` / `instagram_content_publish` scope).

## Backlog cleanup already happened today
An earlier Claude session today marked rows `gen-2026-07-20-*` through `gen-2026-07-27-*` (7 rows) as `ig_failed` with `error_message = "skipped-stale-backlog: IG business account was unlinked from the FB Page ~2026-05-28..."`. This prevents bulk republishing of stale content when the IG link is eventually restored. Rows for 2026-07-28 and 2026-07-29 were **left alone** as the most-recent candidates worth mirroring if the link comes back within 24-48h.

## No new fix PR this run
Ten prior `claude/fix-content-cron-*` observability PRs are open and unmerged: **#166, #167, #170, #171, #173, #174** (plus #151, #153, #159, #161, #162, #163, #165, #168, #169 for older iterations). All propose the same class of fix: call `markFailed` on pending rows when `fetchPageContext` throws so the failure surfaces in `error_message` instead of returning HTTP 200 silently. An 11th duplicate would only add review noise. **PR #174 is the strongest** (cleanest diff, clearest write-up) — that's the one to merge.

Production is still on `8a63851` (2026-06-02 main HEAD). No IG fix has ever shipped.

## What only Kevin can fix
1. **Merge PR #174** (or any of the observability PRs) so tomorrow's cron writes a concrete Meta error into `error_message` instead of failing silently.
2. **Re-link IG Business Account `17841442248205840` to FB Page `1086292991223812`** in Meta Business Suite → Business Assets, OR re-issue `META_MARKETING_API_TOKEN` with `instagram_basic` + `instagram_content_publish` + `pages_show_list` + `pages_manage_posts` scopes and update the Vercel env var.
3. Manually reset today's row if desired (once IG is restored): `UPDATE generated_posts SET ig_published_at = null, error_message = null WHERE slug = 'gen-2026-07-29-59bnh';`

## Live-post spot-check
`facebook.com/122118395102989743` returns 403 (Facebook blocks unauthenticated scrapes; documented in 2026-05-13 log). FB post_id is well-formed `<page_id>_<post_id>` matching the successful publish path; taking that as proof of publish.
