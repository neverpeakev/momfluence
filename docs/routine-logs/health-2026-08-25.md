# 2026-08-25 content cron health check

**Supervisor run:** 2026-08-25 14:46 UTC

## Today's row

| field | value |
| --- | --- |
| id | `b14be98f-1adb-40c9-be77-0239f60dcf28` |
| slug | `gen-2026-08-25-2bu43` |
| angle_tag | `book-fair-checkout-line-book-light-rec` |
| display | *"She named the book light / in the book fair checkout line."* |
| status | `fb_published` |
| fb_post_id | `1086292991223812_122121021824989743` |
| fb_published_at | `2026-08-25 14:01:10 UTC` |
| ig_media_id | `null` |
| ig_published_at | `null` |
| error_message | `null` |

## FB path — healthy

`/api/social/fb-daily-generate` ran on time (Vercel edge log at
14:00:42), inserted the row, and `markFbPublished` fired at 14:01:10. FB
post ID confirmed. No JSON-parse retries today (the regression that lost
2026-08-24 did not repeat).

Prompt v6 (PR #204) is not yet merged; today's `angle_tag` is
`book-fair-checkout-line-book-light-rec` and the display line still
opens `"She named the ..."` — the exact rut PR #204 targets. Format
monoculture continues.

## IG mirror — day 29 of silent no-op

`/api/social/ig-mirror` ran at 14:16:04 UTC (Supabase `edge_logs`
confirms it queried `generated_posts` with the `listPendingIgMirror`
filter and got HTTP 200). Zero subsequent `PATCH /rest/v1/generated_posts`
requests, meaning the endpoint returned 200 without either publishing or
marking the row failed. Vercel serverless logs are empty for this route.

This is the same silent `fetchPageContext` failure that has stranded one
row per day since **2026-07-28**. Today's row makes at least **29** rows
in `fb_published` limbo. Underlying cause is Meta-side: the Instagram
Business Account is not resolvable from FB Page `1086292991223812` via
the current `META_MARKETING_API_TOKEN` — either the link was removed in
Meta Business Suite or the token lost `instagram_basic` /
`instagram_content_publish` scope.

## No new code fix from this session

The existing stack of open draft PRs already covers everything a code
change could fix. Adding a 21st competing PR would only fragment review
further.

| PR | branch | what it does |
| --- | --- | --- |
| **#202** | `claude/fix-content-cron-2026-08-22` | mark rows `ig_failed` when `fetchPageContext` throws; add `console.log`/`console.error` at every outcome. Cleanest fix; recommended merge. |
| #200 | `claude/fix-content-cron-2026-08-20` | overlapping observability + IG lookup change |
| #199 | `claude/fix-content-cron-2026-08-19` | earlier variant of the silent-failure fix |
| #197 | `claude/fix-content-cron-2026-08-17` | earlier variant of the silent-failure fix |
| #195 | `claude/fix-content-cron-2026-08-16` | earlier variant of the silent-failure fix |
| #193 | `claude/fix-content-cron-2026-08-14` | earlier variant of the silent-failure fix |
| #191, #187, #181, #177 | | still-earlier variants of the same fix |
| #198 | `claude/fix-content-cron-2026-08-18` | fixes the FB-side JSON-parse regression that lost 2026-08-24 |
| #204 | `claude/prompt-tune-2026-08-23` | Prompt v6 (fixes `"She named the ..."` monoculture) |

**Recommended action, unchanged from routine logs 2026-08-04 → 2026-08-24:**

1. Merge **PR #202** (or one earlier equivalent) to make future IG-mirror
   failures visible instead of silent. All later PRs #203, #205, and this
   log can then close as documentation.
2. Merge **PR #198** to keep FB-side generation robust against Claude
   emitting raw control characters in JSON strings.
3. Merge **PR #204** for prompt-format rotation, since 4+ weeks of
   `anecdote` / `"She named the ..."` posts are now visible in the feed.
4. **Meta-side (only Kev can do this):** re-link the IG Business Account
   to FB Page `1086292991223812` in Meta Business Suite → Business
   Settings → Accounts → Instagram, and confirm
   `META_MARKETING_API_TOKEN` still carries `instagram_basic`,
   `instagram_content_publish`, `pages_show_list`. Until this is done,
   IG mirroring cannot resume regardless of what code ships.
5. Decide the fate of the ~29 stranded `fb_published` rows: either bulk
   reset to `fb_published` after (4) and let the daily cron drain them
   (respecting the IG 25/24h publish ceiling — a backlog drain will need
   throttling), or accept the loss and bulk-mark them `ig_failed` with a
   `stale-backlog` note.

## PR/log backlog is itself the signal

20 open draft PRs from this routine, going back to 2026-07-30, all
describe the same failure. The routine has been standing watch as
designed; the outputs are not being consumed. Escalating today via
`PushNotification` in addition to the log.
