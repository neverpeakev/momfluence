# Daily content cron health — 2026-08-10

Status: **degraded (chronic — IG mirror silent-failure, day 15)**

FB publish healthy. IG mirror ran on schedule but silent-failed exactly like every day since 2026-07-28. One new row today, backlog now **14** stuck `fb_published` rows (was 13 yesterday).

## Today's row
- slug: `gen-2026-08-10-lou93`
- angle_tag: `school-cafeteria-lunchbox-ice-pack-rec`
- display: "She named the ice pack / at the lunch table."
- fb_post_id: `1086292991223812_122119667306989743` — fb_published_at 14:01:59 UTC
- ig_media_id: `null` — ig_published_at `null` — error_message `null` — errored_at `null`

## Vercel evidence
IG mirror at 14:15:08 UTC:
```
14:15:08 GET /api/social/ig-mirror 200 [info/edge-middleware]
dep=dpl_Gvbxxuy1j3CFJGHXUTDCRSWMouVS branch=main cache=BYPASS
```
Zero function-level entries. Runtime Errors dashboard empty. Identical shape to 2026-07-28 → 2026-08-09.

## Root cause (unchanged since 2026-05-28)
Two problems, only one fixable in code — same as PR #187's diagnosis.

**Operator-side (blocks all IG publishes, cannot fix in code):** Instagram Business Account `17841442248205840` is unlinked from FB Page `1086292991223812`. Kevin — please re-link in Meta Business Suite → Settings → Linked Accounts.

**Code-side (blocks failure visibility, PR #187 unmerged):** `app/api/social/ig-mirror/route.ts` returns HTTP 200 + `{ok:false,message}` on account-level failure, with no `console.error`. Every failure invisible. PR #187 (2026-08-09) makes every failure path loud + marks rows `ig_failed` — still open, still draft, still unmerged. Production is still on `8a63851`, pre-fix.

## Not opening a duplicate PR
This is the 15th consecutive day of the same failure. PR #187 already covers the code fix. Not stacking a #189 that would just duplicate it. Adding a data-point comment to PR #187 instead.

## Backlog
14 rows `fb_published` with `ig_published_at IS NULL`, oldest 2026-07-28, newest today. Per 2026-07-29 audit, these are NOT retroactively republished when IG re-links — that would be a separate explicit action.
