# Daily content cron health check — 2026-06-26

**Status:** `degraded` — FB OK, IG silently failing for 30 days running. Action needed: human Meta re-auth.

## Today's run

- `fb-daily-generate` (14:00 UTC) — **succeeded**
  - Row `gen-2026-06-26-ci6ca`, angle `soccer-practice-sideline-chair-rec`
  - Display: *"She unfolded her chair / and four soccer moms asked the brand."*
  - `fb_post_id = 1086292991223812_122115222926989743`, `fb_published_at = 14:01:31 UTC`
- `ig-mirror` (14:15 UTC) — **silently failed** (same pattern as the last 30 days)
  - Vercel edge log: `14:15:08 GET /api/social/ig-mirror 200`
  - No serverless log line, no `console.error`, no `runtime_errors` entry in the window
  - DB row still `status='fb_published'`, `ig_media_id=null`, `error_message=null`

## Backlog state

| Metric | Value |
|---|---|
| Rows stranded at `fb_published` (no IG) | **25** |
| Oldest stranded | 2026-05-29 14:01 UTC |
| Newest stranded | 2026-06-26 14:01 UTC |
| Days since last successful IG publish | 30 |

Backlog grew by 2 since PR #134 was opened (23 → 25) — confirms the silent-fail pattern is unchanged.

## Root cause (unchanged from PR #134)

External Meta Graph API auth on the IG side. The `META_MARKETING_API_TOKEN` still publishes to FB Page `1086292991223812` daily, but IG calls fail at `fetchPageContext` or earlier with the same `error_subcode 33` `GraphMethodException` documented in PR #134.

**Most likely:** IG-Business-Account `17841442248205840` is unlinked from FB Page `1086292991223812` in Business Settings, **or** the token's `instagram_basic` / `instagram_content_publish` scopes were dropped.

## Open PRs blocking on this

| PR | Title | State |
|---|---|---|
| [#134](https://github.com/neverpeakev/momfluence/pull/134) | Fix daily content cron: IG mirror silently failing for 27 days | **Open draft, not merged** — would make the failure visible in `error_message` and return 500 so this routine could diagnose same-day |
| [#135](https://github.com/neverpeakev/momfluence/pull/135) | Health log 2026-06-24 | Open draft |
| [#136](https://github.com/neverpeakev/momfluence/pull/136) | Fix daily content cron: Claude max_tokens 1500→4000 | Open draft (FB-side; today's FB run succeeded but tail risk remains) |

## Required human action

1. **Re-link IG in Business Settings:** FB Business Settings → Pages → Momfluence (`1086292991223812`) → Instagram accounts → confirm `@momfluence` (`17841442248205840`) is linked. If not, re-link.
2. **Verify token scopes:** Business Settings → System Users → owner of `META_MARKETING_API_TOKEN` → Assets → confirm FB Page + IG account assigned; scopes include `instagram_basic`, `instagram_content_publish`, `pages_show_list`, `pages_read_engagement`, `pages_manage_posts`, `business_management`.
3. **If scopes were dropped:** generate a new long-lived system-user token, update Vercel env `META_MARKETING_API_TOKEN` for production, redeploy.
4. **Merge PR #134** so future silent failures show up in `error_message` and trip cron alarms (independent of the re-auth — pure observability win).

After (1)–(3), `listPendingIgMirror` caps at 10/day and IG limits to 25 publishes per 24h, so the 25-row backlog drains over 3 days, well under the limit.

## No new PR opened today

The diagnosis, code fix, and recovery plan are all already on PR #134. Opening a duplicate would only add noise. This log links there.

## Verification skipped

- FB post live-check via `WebFetch https://facebook.com/momfluence/posts` not attempted — the page requires a login redirect and isn't public-readable; the Graph API returning a valid `fb_post_id` is the stronger signal that FB accepted the publish.
