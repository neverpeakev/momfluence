# Optimizer routine review — 2026-05-28 12:00 UTC tick

Status: **cron healthy — but campaign has been dark for 7 days**

Reviewed ~4h after the 12:00 UTC tick. The optimizer is firing on schedule and
behaving correctly. The notable finding is upstream of the optimizer: the Meta
campaign has had **$0 spend and zero traffic since 2026-05-22**, so every tick
for the past week has had nothing to optimize.

## Tick under review
- Tick ID: `87c84812-c707-4a10-8afd-20a31e20293e`
- occurred_at: `2026-05-28 12:00:34 UTC` (4.2h before this review — within the 8h freshness window)
- mode: `shadow`
- duration_ms: `2197`
- variants_seen: `10` · ads_seen: `45` · signups_seen: `1`
- data_pull_ok: `true` · error_message: none

## Decisions this tick
| decision | count |
|----------|-------|
| NO-OP    | 10    |
| PAUSE    | 0     |
| SCALE    | 0     |
| REMIX    | 0     |

All 10 variants returned NO-OP with the same rationale: *"Insufficient data —
visits=0 (need ≥100), conv=0 (need ≥5)."* Every variant shows visits=0,
conv=0, spend=$0.00, and a flat posterior (p_best ≈ p_worst ≈ 0.10, i.e. the
uniform 1/10 prior — no variant has separated from the pack). Nothing
actionable, so no shadow decisions to review for *this* tick.

## Headline: the campaign isn't spending

This is not day-1 "awaiting data" (we're 17 days in). The optimizer is healthy;
the ad campaign has gone dark. Daily roll-up from `optimizer_actions`:

| day        | visits | conv | spend     | max visits/variant |
|------------|--------|------|-----------|--------------------|
| 2026-05-11 | 35,799 | 1038 | $2,499.00 | 318                |
| 2026-05-12 | 2      | 0    | $0.67     | 1                  |
| 2026-05-13 | 8      | 0    | $2.90     | 2                  |
| 2026-05-14..19 | 16–20 | 0 | ~$4.68/d  | 3                  |
| 2026-05-20 | 12     | 0    | $1.78     | 2                  |
| 2026-05-21 | 4      | 0    | $0.14     | 2                  |
| **2026-05-22 → 05-28** | **0** | **0** | **$0.00** | **0** |

The entire ~$2,500 budget was spent on launch day (May 11), traffic ran on
fumes for a week, and from **May 22 onward the campaign has been completely
dark** — $0/day, 0 visits, 0 conversions, 7 days running. The tick-level
`ads_seen=45 / signups_seen=1` has been byte-identical for the same stretch:
those are static counts of configured ad objects + one historical signup, not
new activity. `data_pull_ok=true` and varying `duration_ms` (1.4–2.5s)
indicate the optimizer is making real Meta calls each tick — it's just getting
back an idle account.

## Has the engine ever fired? Yes — once, on launch day

All-time action counts:
- `no_op`: 814
- `scale`: 9 — all on 2026-05-11 (08:52 → 19:10)
- `remix_proposed`: 9 — all on 2026-05-11
- `tick_error`: 3

During the May 11 traffic burst the optimizer cleanly identified a winner:

> **group-chat-goldmine** — P(best)=0.998, 300 visits / 24 conv, CPA $0.88.
> SCALE rationale: *"P(best)=0.998 ≥ threshold 0.8 after 300 visits / 24 conv.
> Bump daily budget +30% to $39.00 (cap 3× starting)."*
> REMIX rationale: *"Clear leader (P(best)=0.998). Queueing 3 LLM-generated
> remix candidates for admin review."*

The 9 SCALE / 9 REMIX rows are the same decision re-emitted across that day's
ticks (it stayed the leader). All were **shadow-mode, so nothing was enacted**.
Note: `proposed_copy` was NOT persisted for the remix rows (column is null) —
the remix candidate copy referenced in the rationale isn't stored in the DB.

### Shadow decisions — what live mode WOULD have done (historical, May 11)
Had mode been `live` on launch day, the optimizer would have:
1. Raised **group-chat-goldmine**'s daily budget +30% → $39.00 (capped at 3× start).
2. Queued 3 LLM-generated remix variants of that angle for admin review.

No other variant ever crossed the P(best) ≥ 0.8 threshold.

## Errors observed (`tick_error`)
- 2026-05-11 12:00 & 18:00 — *"Meta not configured: missing META_AD_SET_ID."*
  Early config gap on launch day; resolved (no recurrence after May 11).
- 2026-05-22 06:00 — *"Meta pull failed: Meta API …/insights → 400 Service
  temporarily unavailable (OAuthException, subcode 1504044, is_transient=false)."*
  Single occurrence, coincides with the start of the dark period. Subsequent
  ticks report `data_pull_ok=true`, so the pull itself recovered — but the
  account it reads has been idle since.

## Assessment & action items
- **Cron / optimizer code: healthy.** Ticks firing every 6h on schedule, latest
  4.2h ago, no current errors. **No code fix and no PR opened** (per runbook,
  PRs are only for a broken cron).
- **For Kevin (human decision — not auto-actionable):**
  1. The Meta campaign has not spent in 7 days. **Re-fund / re-activate it, or
     confirm it's intentionally paused.** The optimizer cannot do anything with
     zero traffic — every future tick will keep returning NO-OP until spend resumes.
  2. We're 17 days in and still in `shadow`. The engine proved it works on May 11
     (cleanly picked group-chat-goldmine). Once traffic resumes, decide whether
     to flip to `live` so winning variants actually scale. *(Mode left untouched —
     that's the human's call.)*
  3. Minor: `proposed_copy` isn't being persisted for `remix_proposed` actions —
     worth wiring up so remix candidates are auditable, but not urgent while dark.
