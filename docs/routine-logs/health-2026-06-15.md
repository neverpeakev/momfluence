# Daily content cron health — 2026-06-15

Status: **DEGRADED — day 8 of the silent IG-mirror failure streak**

FB published cleanly. IG mirror returned HTTP 200 at 14:15:08 UTC, wrote nothing
to the database, and produced no Vercel runtime log output. Identical symptom
profile to 2026-06-09, 06-10, 06-11, 06-13, and 06-14. Today's row is stuck in
`fb_published` with `error_message=NULL`, indistinguishable from the healthy
mid-cron state.

## Today's row
- slug: `gen-2026-06-15-fpu01`
- angle_tag: `urgent-care-waiting-room-humidifier-rec`
- display: "She told the coughing kid's mom / which humidifier actually works."
- status: `fb_published`
- fb_post_id: `1086292991223812_122114128400989743` (fb_published_at 14:02:04 UTC) ✅
- ig_media_id: `NULL` ❌
- error_message: `NULL` (no diagnostic surface)
- errored_at: `NULL`

## 9-day picture (from Supabase)

```
2026-06-07  fb_published   ig_published_at=NULL   error_message=NULL
2026-06-08  fb_published   ig_published_at=NULL   error_message=NULL
2026-06-09  fb_published   ig_published_at=NULL   error_message=NULL
2026-06-10  fb_published   ig_published_at=NULL   error_message=NULL
2026-06-11  fb_published   ig_published_at=NULL   error_message=NULL
2026-06-12  fb_failed      transient Graph 5xx (fixed by PR #120, also unmerged)
2026-06-13  fb_published   ig_published_at=NULL   error_message=NULL
2026-06-14  fb_published   ig_published_at=NULL   error_message=NULL
2026-06-15  fb_published   ig_published_at=NULL   error_message=NULL  ← today
```

## Open PRs from prior routines — all unmerged

| PR | Opened | Branch | State |
|---|---|---|---|
| [#119](https://github.com/neverpeakev/momfluence/pull/119) | 2026-06-11 | `claude/fix-content-cron-2026-06-11` | open / draft / mergeable / **unmerged** |
| [#120](https://github.com/neverpeakev/momfluence/pull/120) | 2026-06-12 | `claude/fix-content-cron-2026-06-12` | open / draft / **unmerged** (FB 5xx retry) |
| [#121](https://github.com/neverpeakev/momfluence/pull/121) | 2026-06-13 | `claude/fix-content-cron-2026-06-13` | open / draft / mergeable / **unmerged** (same diff as #119) |
| [#122](https://github.com/neverpeakev/momfluence/pull/122) | 2026-06-14 | `claude/fix-content-cron-2026-06-14` | open / draft / mergeable / **unmerged** (logging + `markFailed` on page-context throw) |

Deployed `app/api/social/ig-mirror/route.ts` (commit 8a63851, deployment
`dpl_Gvbxxuy1j3CFJGHXUTDCRSWMouVS`) still has no `console.*` calls on any
failure branch and still returns 200 from every catch except `authorized()`.
None of the four diagnostic fixes have shipped.

## No new PR opened today

Stacking a 4th identical PR (`claude/fix-content-cron-2026-06-15`) would not
help — the diff already exists in three open PRs. The blocker is review/merge,
not authoring. **PR #122 is the recommended one to merge** — it includes
PR #119's logging fix *and* additionally calls `markFailed` for each pending
row when `fetchPageContext` throws, which would have surfaced the underlying
Meta-side error a week ago.

## Diagnosis (unchanged from #119/#121/#122)

Vercel runtime logs for today, 14:15:08 UTC:
- `GET /api/social/ig-mirror → 200` — no `level`, no message body.
- No follow-up `/api/render/post/...png` warm-fetch (that fires at L198 of
  the route, inside the per-row loop), so execution bailed before the loop.
- No `console.error` from the page-context catch (L182-192).

The row matches `listPendingIgMirror`'s filter exactly
(`fb_published_at IS NOT NULL AND ig_published_at IS NULL AND status='fb_published'`),
so `pending` was non-empty. The only path consistent with the trace is
`fetchPageContext` throwing on the Graph `/me/accounts` or
`/{pageId}?fields=instagram_business_account` call — and its catch returns
`NextResponse.json({ ok: false, ... }, /* default 200 */)` with no logging.

Likely underlying Meta-side cause (unverifiable until #122 merges and the next
run logs the actual Graph error): expired/revoked Page Access Token, broken
`instagram_business_account` link to FB Page `1086292991223812`, or sustained
Graph 5xx. The `META_MARKETING_API_TOKEN` itself was working 13 minutes
earlier for the FB cron, so a generic expiry is less likely than a scope or
IG-link issue.

## Live-post spot-check

`WebFetch https://facebook.com/momfluence/posts` returned 403 (Facebook blocks
unauthenticated scrapes — same as 2026-05-13). Treating the well-formed
`fb_post_id` `1086292991223812_122114128400989743` plus the successful FB cron
trace as proof of publish for the FB side.

## Recommended operator action

1. Merge **PR #122**.
2. After deploy lands, manually re-trigger:
   `curl -H "Authorization: Bearer $CRON_SECRET" https://momfluence.app/api/social/ig-mirror`
3. Read the Vercel runtime log for the now-visible Graph API error and fix
   the underlying Meta-side issue (token refresh, re-link IG account, etc.).
4. Reset today's row to retry IG (or accept FB-only for the 9-day backlog).
5. Close the duplicate PRs #119 and #121 once #122 ships.
