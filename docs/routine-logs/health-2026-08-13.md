# Daily content cron health — 2026-08-13

Status: **fb-only (IG mirror silent-failure day 18)**

## Today's row

- id: `2e82be1d-f9b1-4cd2-855b-a7eed3e146fa`
- slug: `gen-2026-08-13-2hhcv`
- angle_tag: `church-nursery-pacifier-clip-rec`
- display: "She named the pacifier clip / in the church nursery."
- status: `fb_published`
- fb_post_id: `1086292991223812_122119945946989743`
- fb_published_at: `2026-08-13 14:02:19 UTC` ✓
- ig_media_id: `null`
- ig_published_at: `null`
- error_message: `null` (silent failure — no row-level error recorded)
- errored_at: `null`

## IG mirror invocation

Same shape as every day since 2026-07-28: FB row was written at
14:02:19 UTC, ig-mirror cron scheduled for 14:15 UTC. Health check
ran at 14:47 UTC (32 min after mirror should have fired). Row
unchanged — `status=fb_published`, `ig_media_id=null`,
`error_message=null`, `errored_at=null`. Zero function-level output
in the observed window. Vercel runtime log query for
`/api/social/ig-mirror` timed out on the 24h scan, but the row-state
fingerprint is identical to days 3–17 of this pattern (all
documented in prior PR bodies).

## Backlog

```
backlog_size: 17
oldest_stuck: 2026-07-28 14:02:04 UTC
newest_stuck: 2026-08-13 14:02:19 UTC
```

Was 16 yesterday, +1 today. Last successful IG publish: 2026-05-27
(78 days ago).

## Not opening a duplicate code PR

Production is still on commit `8a63851` (2026-06-02, #108 embedded
checkout). Every observability fix PR opened since 2026-07-31 is
still open, draft, and unmerged:

- **#191** (`claude/fix-content-cron-2026-08-12`) — the most recent
  and cleanest. On `fetchPageContext` failure: `console.error` the
  wrapped error AND `markFailed(row.id, "ig", ...)` for every
  already-fetched pending row. Response still returns 200 with the
  message body, so cron caller behavior is unchanged; only the side
  effects are new. **Merge this.**
- #187 — earlier stronger variant (also flips missing-env and
  per-row failures to HTTP 500). Wider blast radius on the response
  status; #191 is the smaller, safer diff.
- #181, #177 — older variants covered by #191.

Opening a 13th `claude/fix-content-cron-*` PR duplicates the
merge-me ask that #191 already carries and would only add noise.
Same call as the 2026-08-10 / 2026-08-11 / 2026-08-12 routines.

## Owner action (unchanged)

1. Merge **#191** so tomorrow's failure surfaces a real error on the
   row (`error_message` populated) and stops the backlog growing
   past the current 17.
2. Re-link IG Business Account `17841442248205840` to FB Page
   `1086292991223812` in Meta Business Suite → Business Assets →
   Instagram Accounts. Re-authorize the system-user token with
   `instagram_basic` + `instagram_content_publish` +
   `pages_show_list` + `pages_manage_posts`, then refresh
   `META_MARKETING_API_TOKEN` in Vercel env.
3. Decide the 17-row backlog **before** re-triggering the mirror —
   otherwise a fresh IG link plus a working mirror will bulk-publish
   17 stale posts (2026-07-28 → 2026-08-13) in one run:

   ```sql
   -- Option A: skip the backlog, publish only fresh rows going forward
   UPDATE public.generated_posts
   SET status = 'ig_failed',
       error_message = 'skipped-stale-backlog-2026-05-28',
       errored_at = now()
   WHERE status = 'fb_published'
     AND ig_media_id IS NULL
     AND fb_published_at < '2026-08-13';
   ```

   Skips the 16 pre-today rows; keeps `gen-2026-08-13-2hhcv` eligible
   for the next mirror tick.

## Live-post spot-check

WebFetch of `facebook.com/momfluence/posts` blocked by the network
egress proxy (`EGRESS_BLOCKED`, same as every prior daily log).
`fb_post_id 1086292991223812_122119945946989743` is well-formed
(`<page_id>_<post_id>`, valid Meta Graph ID shape) and the row was
written via the FB publish path — `markFbPublished` is only reached
after a successful Graph API POST returns a real post id, so
existence on the page is high-confidence without the visual check.

## Related

- Same-shape logs on their PR branches: #191 (2026-08-12), #190
  (2026-08-11), #189 (2026-08-10), #186 (2026-08-08), #185
  (2026-08-07), #184 (2026-08-06), #183 (2026-08-05), #182
  (2026-08-04), #179 (2026-08-02), #178 (2026-08-01).
- Code fixes waiting to ship: **#191** (best), #187, #181, #177.
