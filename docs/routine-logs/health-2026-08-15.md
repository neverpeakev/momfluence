# Daily content-cron health — 2026-08-15

**Status:** FB ok, IG mirror silently failing (day 12+ of ongoing outage).

## Today's row

- slug: `gen-2026-08-15-y0elu`
- angle_tag: `dentist-waiting-room-floss-picks-rec`
- display: *"She named the floss picks / in the dentist waiting room."*
- fb_post_id: `1086292991223812_122120129564989743` (published 14:02:01 UTC)
- ig_media_id: **null** — status stuck at `fb_published`, no `error_message`

## Root cause (unchanged since 2026-07-29 investigation)

The IG Business Account was unlinked from FB Page `1086292991223812` around
2026-05-28. `fetchPageContext` in `app/api/social/ig-mirror/route.ts` throws
"No Instagram Business Account linked to FB Page 1086292991223812". The route
catches this at line 182–192 and returns 200 with `ok:false, message:
"auth/page-context: ..."` — but never calls `markFailed`, so the DB row stays
`fb_published` with no error trail. Vercel runtime logs only show the outer
200; the JSON body is not persisted.

12 consecutive daily FB posts (2026-08-04 → 2026-08-15) sit in this
half-published state. The three rows from 2026-07-28/29 were manually
back-filled to `ig_failed` by a prior Claude session; the daily accumulation
resumed the next day.

## Action required (human)

Relink the Instagram Business Account (id `17841442248205840`) to FB Page
`1086292991223812` in Meta Business Suite → Business Assets. Cron will
resume mirroring the next day; no code change required.

## Latent code bug (not blocking, not fixed today)

`ig-mirror/route.ts` should call `markFailed` on the pending rows when
`fetchPageContext` throws, so the DB reflects reality and this routine can
alert on the first failure instead of catching it via a status-count drift
check. Deferred — filing as an issue would help; PR skipped because the real
fix is the manual relink and a code change now would only rewrite 12 stale
rows to `ig_failed`.

## Steps skipped / limitations

- FB post visibility spot-check: `facebook.com` is blocked by this
  environment's network egress proxy. Publication is inferred from the
  Meta-returned `fb_post_id`.
- No PR opened — root cause is a manual Meta config fix, not a code defect.
