# Daily content cron health — 2026-07-12

**Status:** FB healthy, IG mirror still silently failing (day 47 since last IG success).

## Today's row

- `gen-2026-07-12-01pii` (angle_tag `back-to-school-night-planner-rec`)
- display: *"She named the planner / in the folding chair."*
- fb_post_id `1086292991223812_122116857140989743`, fb_published_at `14:01:09 UTC` ✅
- ig_media_id `null`, ig_published_at `null`, status `fb_published`, error_message `null` ❌

Same failure signature as every day since 2026-05-28 (47 rows now stuck in `fb_published`
with no `error_message`). Vercel runtime log shows `GET /api/social/ig-mirror 200` at
`14:15:08 UTC` with no error output. Supabase API logs confirm `listPendingIgMirror`
executed at that timestamp and returned 200 (rows matched), but no follow-up PATCH on
`generated_posts` — i.e. the endpoint exited before the per-row loop. Still the silent
`auth/page-context` early return.

## FB post spot-check

Skipped — `https://facebook.com/momfluence/posts` returns HTTP 403 to unauthenticated
scrapers (confirmed on 2026-07-11). The FB Graph write-back (`fb_post_id` populated, no
`error_message`) is the authoritative signal that FB accepted the post.

## Not opening a new PR

Both draft fix PRs are still open on `main`, both `mergeable_state=clean`, both
targeting base sha `8a63851`:

- **PR #151** — https://github.com/neverpeakev/momfluence/pull/151
  (branch `claude/fix-content-cron-2026-07-08`, opened 2026-07-08, 4 days idle)
  Marks the pre-loop `auth/page-context` failure as `ig_failed` per row *and* adds
  `console.error` at both failure branches. The more complete fix.
- **PR #153** — https://github.com/neverpeakev/momfluence/pull/153
  (branch `claude/fix-content-cron-2026-07-10`, opened 2026-07-10, 2 days idle)
  Observability-only follow-up.

Either one, merged, would surface the actual Meta error in Vercel Runtime Logs on the
next 14:15 UTC cron. Opening a fourth PR (this would be the fourth fix branch across
`claude/fix-content-cron-2026-07-08/-10`) would just add noise — the block is a merge
decision, not more code.

## Suspected root cause (unchanged from PRs #151 / #153)

`fetchPageContext` throws before the row loop. Almost certainly Meta scope loss on
`META_MARKETING_API_TOKEN` — the last successful `ig_published` was 2026-05-27 and the
adjacent `ig_failed` rows (05-23…-26) carry Meta `code=100, subcode=33`
(GraphMethodException / missing permissions), classic for lost `instagram_basic` +
`instagram_content_publish`.

## Ask

Merge PR #151 (or #153) → wait for the 2026-07-13 14:15 UTC cron → read the
`[ig-mirror] auth/page-context failed:` line in Vercel Runtime Logs → regenerate
`META_MARKETING_API_TOKEN` with `instagram_basic`, `instagram_content_publish`,
`instagram_manage_insights`, `pages_show_list`, `pages_manage_posts` (or re-link the
IG Business Account `17841442248205840` to FB Page `1086292991223812` in Meta Business
Suite if the link dropped).
