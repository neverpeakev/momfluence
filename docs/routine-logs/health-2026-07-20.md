# Daily content cron health — 2026-07-20

**Run at:** 2026-07-20 14:46 UTC (routine)
**FB:** healthy — `gen-2026-07-20-orqyn`, angle `flu-shot-pharmacy-line-lollipop-rec`, display "She named the lollipops / in the flu shot line.", fb_post_id `1086292991223812_122117606864989743`, published 14:02:09 UTC.
**IG:** **still silently failing — day 54.** Row is `status=fb_published`, `ig_media_id=null`, `error_message=null`. Cron fired at 14:15:08 UTC and returned 200 (edge log only, no serverless output).

## Why no code change today

**Six observability-fix PRs are already open and unmerged**, all against the same silent `fetchPageContext` early-return path in `app/api/social/ig-mirror/route.ts`:

| PR | Opened | Age | State |
|---|---|---|---|
| #151 | 2026-07-08 | 12d | open, draft, clean |
| #153 | 2026-07-10 | 10d | open, draft, clean |
| #159 | 2026-07-15 | 5d | open, draft, clean |
| #161 | 2026-07-17 | 3d | open, draft, clean |
| #162 | 2026-07-18 | 2d | open, draft, clean |
| #163 | 2026-07-19 | 1d | open, draft, clean |

Production is still on `8a63851` (main HEAD from 2026-06-02). None of these have merged. Opening a 7th duplicate branch would only add noise — the block is a merge decision + a Meta-side fix, not more code.

## What actually needs to happen (owner action, cannot be automated from this session)

1. **Merge one** of #151 / #153 / #159 / #161 / #162 / #163. Any one is enough — they all add the same `console.error` on the silent auth path so the next 14:15 UTC cron logs the exact Graph-API failure to Vercel Runtime Logs.
2. After the next cron fires post-merge, read the `[ig-mirror] auth/page-context failed: …` line — that's the Meta error we've been blind to.
3. Fix the Meta side — likely one of:
   - Meta Business Suite → Momfluence FB Page → Instagram accounts → re-link IG Business Account `17841442248205840`.
   - System User backing `META_MARKETING_API_TOKEN` → re-grant `instagram_basic` + `instagram_content_publish` scopes on both the FB Page and IG account.
4. Manually POST `/api/social/ig-mirror` with the `CRON_SECRET` bearer 5× to drain the ~45-row backlog (endpoint caps at 10 per invocation).

## Silence-of-the-cron summary

- Last successful `ig_published`: 2026-05-27.
- 54 consecutive days of silent IG failure.
- FB side is 100% healthy — same env token successfully drives `/me/accounts` at 14:00 UTC every day.
- Failure is confined to the `/{pageId}?fields=instagram_business_account` call inside `fetchPageContext`.
