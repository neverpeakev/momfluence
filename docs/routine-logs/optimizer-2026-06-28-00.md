# Optimizer tick — 2026-06-28 00:00 UTC

- **Tick ID:** `0e36386f-eade-48fe-a831-1229911489f0`
- **occurred_at:** 2026-06-28 00:01:05 UTC (~6h before this check — within budget)
- **Mode:** `shadow`
- **duration_ms:** 3761
- **variants_seen:** 10
- **ads_seen:** 45
- **signups_seen:** 2
- **data_pull_ok:** true

## Decisions

| action_type | count |
|---|---|
| no_op | 10 |

All 10 variants returned `no_op` with the same rationale: **insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)**.

Posterior is still effectively flat — `p_best` and `p_worst` for every variant sit in a narrow ~0.095–0.107 band (the prior, essentially untouched). No variant has accumulated any visit or conversion attributable to the optimizer's data pull yet, despite `signups_seen=2` at the tick level (signups not yet joined to per-variant rollups).

**Awaiting data — 0 attributed visits across 10 variants, posterior still flat.** No shadow decisions to surface.

## Notes

- Cron is healthy: tick landed on schedule at 00:01:05 UTC.
- Settings table is keyed (`id`,`value`); current `mode=shadow`. The supervisor doc's `SELECT mode FROM optimizer_settings` query needs updating to `WHERE id='mode'` — minor doc drift, not a runtime issue.
- `optimizer_actions` columns are `action_type` / `variant` / `creative` (not `decision_type` / `variant_slug` / `ad_id` as the supervisor doc says). Same — doc drift only.
- One variant (`move-over-influencers`, c17) has `meta_ad_id=null` — worth a glance next tick if it stays null while others get attributed traffic.
