# Daily content cron health — 2026-07-11

**Status:** FB healthy, IG mirror still silently failing (day 46 since last IG success).

## Today's row

- `gen-2026-07-11-ctyzi` (angle_tag `open-house-classroom-water-bottle-rec`)
- display: *"She named the water bottle / at the classroom sign-in."*
- fb_post_id `1086292991223812_122116760828989743`, fb_published_at `14:01:23 UTC` ✅
- ig_media_id `null`, ig_published_at `null`, status `fb_published`, error_message `null` ❌

Same failure signature as every day since 2026-05-28 (46 rows now stuck in `fb_published`
with no `error_message`). Vercel runtime log shows `GET /api/social/ig-mirror 200` at
`14:15:08 UTC` with no error output — still the silent-`auth/page-context` early return.

## FB post spot-check

WebFetch of https://facebook.com/momfluence/posts returned HTTP 403 (Facebook blocks
unauthenticated scraping); can't confirm from here. FB API round-trip succeeded per DB
(`fb_post_id` populated, no `error_message`) and the FB Graph write-back is the
authoritative signal.

## Not opening a new PR

Two draft fix PRs are already open on `main`, both `mergeable_state=clean`:

- **PR #151** — https://github.com/neverpeakev/momfluence/pull/151
  (branch `claude/fix-content-cron-2026-07-08`, opened 2026-07-08)
  Marks the pre-loop `auth/page-context` failure as `ig_failed` per row *and* adds
  `console.error` at both failure branches.
- **PR #153** — https://github.com/neverpeakev/momfluence/pull/153
  (branch `claude/fix-content-cron-2026-07-10`, opened 2026-07-10)
  Observability-only follow-up: `console.error` on all three pre-loop early returns,
  plus `[ig-mirror] pending=N` after the pending-list query.

Either one, merged, would surface the actual Meta error in Vercel Runtime Logs on the
next 14:15 UTC cron. #151 is the more complete fix — it also updates the DB so future
routine checks can grep `error_message`.

Duplicating a third PR would just add noise. The blocker is a merge decision, not more
code.

## Suspected root cause (unchanged from PRs #151 / #153)

`fetchPageContext` throws before the row loop. Almost certainly Meta scope loss on
`META_MARKETING_API_TOKEN` — the last successful `ig_published` was 2026-05-27 and the
adjacent `ig_failed` rows (05-23…-26) carry Meta `code=100, subcode=33`
(GraphMethodException / missing permissions), classic for lost `instagram_basic` +
`instagram_content_publish`.

## Ask

Merge PR #151 (or #153) → wait for the 2026-07-12 14:15 UTC cron → read the
`[ig-mirror] auth/page-context failed:` line in Vercel Runtime Logs → regenerate
`META_MARKETING_API_TOKEN` with `instagram_basic`, `instagram_content_publish`,
`instagram_manage_insights`, `pages_show_list`, `pages_manage_posts` (or re-link the
IG Business Account `17841442248205840` to FB Page `1086292991223812` in Meta Business
Suite if the link dropped).
