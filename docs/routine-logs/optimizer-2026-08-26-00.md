# Optimizer tick — 2026-08-26 00:00 UTC

## Tick metadata

- **tick_id:** `45f90e35-a32b-4489-ab50-e7aa7bc562b9`
- **occurred_at:** 2026-08-26 00:02:42 UTC
- **mode:** `shadow`
- **duration_ms:** 1933
- **variants_seen:** 10
- **ads_seen:** 45
- **signups_seen:** 2
- **data_pull_ok:** true

## Decisions

| action_type | count |
|---|---|
| `no_op` | 10 |

Awaiting data — 0 visits across all 10 variants, posterior still flat (p_best ≈ 0.096–0.107 uniform, indistinguishable from prior). Every arm gated on the `min_visits_per_arm=100` / `min_conversions_per_arm=5` thresholds; no arm has enough data yet to move.

## Notes

- Two signups landed inside the tick window (`signups_seen=2`) but the per-variant `visits` column is 0 for every arm. Either attribution isn't wiring visits to `optimizer_actions.visits` yet, or those signups landed outside the arm-tagged funnel. Worth watching next tick — if `signups_seen` keeps climbing while per-arm visits stay at 0, the visit-attribution join is broken and the optimizer will never leave no_op.
- Ad `120247149679580346` (`twenty-five-day-one`, c18) has the slightly-highest prior mean (p_best 0.107) — noise, but it's the current front-runner if you squint.
- One row has `meta_ad_id=null` (`move-over-influencers`, c19-labeled c17). Not an error at this stage, but if it stays null the arm will never receive spend.

## Shadow → live

No shadow-decisions to translate — nothing would have been enacted under live mode either.
