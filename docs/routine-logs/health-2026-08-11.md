# Daily content cron health — 2026-08-11

Status: **fb-only (IG mirror silent-failure day 16)**

## Today's row

- id: `ab64220c-7763-4445-940e-3ce4963c3734`
- slug: `gen-2026-08-11-k7frx`
- angle_tag: `pta-meeting-folding-chair-cushion-rec`
- display: "She named the seat cushion / at the PTA meeting."
- status: `fb_published`
- fb_post_id: `1086292991223812_122119764170989743`
- fb_published_at: `2026-08-11 14:01:16 UTC` ✓
- ig_media_id: `null`
- ig_published_at: `null`
- error_message: `null` (silent failure — no row-level error recorded)
- errored_at: `null`

## IG mirror invocation

Vercel runtime log for `/api/social/ig-mirror` in the 14:15–14:20 UTC window:

```
14:16:03 GET /api/social/ig-mirror 200 [info/edge-middleware]
```

Same shape as every day since 2026-07-28: 200 from edge-middleware, zero
function-level output, no Runtime Errors entry, row untouched.

## Backlog

15 rows now stuck at `status=fb_published` with `ig_media_id=null`
(was 14 yesterday, +1 today). Last successful IG publish: 2026-05-27.

## Not opening a duplicate code PR

Two structural fixes are still open, draft, unmerged:

- **#187** (`claude/fix-content-cron-2026-08-09`) — makes every failure path
  loud and permanent (missing env → HTTP 500 + console.error; page-context
  failure → HTTP 500 + mark all pending rows `ig_failed`; per-row failure
  → console.error). Landing this would eliminate the silent-failure shape.
- **#181** (`claude/fix-content-cron-2026-08-03`) — lighter observability
  fix (three `console.*` lines).

Production is still on commit `8a63851` (2026-06-02, `#108` embedded
checkout). Neither observability PR has ever shipped. Opening a 12th
`claude/fix-content-cron-*` PR duplicates the merge-me ask that #187
already carries, so this log is just a data-point PR (matches the
pattern PR #189 established yesterday).

## Owner action (unchanged)

1. Merge **#187** so tomorrow's failure surfaces a real error.
2. Re-link IG Business Account `17841442248205840` to FB Page
   `1086292991223812` in Meta Business Suite → Business Assets →
   Instagram Accounts. Re-authorize the system-user token with
   `instagram_basic` + `instagram_content_publish` + `pages_show_list`
   + `pages_manage_posts`, then refresh `META_MARKETING_API_TOKEN` in
   Vercel env.
3. Decide the 15-row backlog before re-triggering: bulk-publish (blasts
   stale content) or mark them all `ig_failed` with
   `error_message='skipped-stale-backlog-2026-05-28'` so the next
   ig-mirror tick only picks up fresh rows.

## Live-post spot-check

Skipped WebFetch of facebook.com/momfluence/posts — prior logs confirm
Facebook blocks unauthenticated scrapes with 403. `fb_post_id`
`1086292991223812_122119764170989743` is well-formed (`<page_id>_<post_id>`)
and the row was written by the FB publish path, which markFbPublished
only reaches after a successful Graph API response.

## Related

- Prior daily logs: `docs/routine-logs/health-2026-05-13.md` (only
  committed one in-tree); all subsequent daily logs live on their
  respective `claude/routine-log-*` PR branches.
