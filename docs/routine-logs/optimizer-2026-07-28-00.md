# Optimizer supervisor log — 2026-07-28 00:00 UTC tick

- **Tick ID:** `a42fc682-03e7-4f48-9fb9-54492ffde45d`
- **Occurred at:** 2026-07-28 00:00:59 UTC
- **Mode:** shadow
- **Duration:** 1,814 ms
- **Variants seen:** 10
- **Ads seen:** 45
- **Signups seen (tick-level):** 2
- **Cron health:** OK — checked at 04:08 UTC (~4h after tick).

## Decision counts

| type | count |
|------|-------|
| no_op | 10 |
| PAUSE | 0 |
| SCALE | 0 |
| REMIX | 0 |

All 10 variants held. No shadow decisions to translate this tick.

## Status

Awaiting data — 0 visits attributed across all 10 variants; posteriors still flat at ~p_best 0.09–0.11 (uniform prior). Thresholds are `min_visits_per_arm=100`, `min_conversions_per_arm=5`, so nothing is remotely close to a call yet.

## Observation to flag on next tick

Tick-level `signups_seen=2` while per-variant `visits=0` on every arm. That's consistent with signups landing before variant attribution catches up (first tick after launch is expected to be sparse), but if the same asymmetry appears at the 06:00 UTC tick it suggests the variant-attribution join is missing rows — worth checking the attribution query then. Not acting on it this tick.

One variant (`move-over-influencers` / c17) has `meta_ad_id=null` in the action row — likely just means the Meta ad wasn't matched at pull time; re-check next tick.
