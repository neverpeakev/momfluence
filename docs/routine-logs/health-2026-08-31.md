# Daily content cron health — 2026-08-31

Status: **degraded (known) — FB path healthy, IG mirror silently no-op'd (day 35)**

## FB path healthy

- slug: `gen-2026-08-31-kr21f`
- angle_tag: `farmers-market-parking-lot-reusable-produce-bag-rec`
- display: "She named the produce bags / in the farmers market parking lot."
- fb_post_id: `1086292991223812_122121523436989743`
- fb_published_at: 14:04:12 UTC

Note: display still opens with "She named the ___" — same anecdote-monoculture pattern PR #211 (prompt v6) was opened to break; #211 remains unmerged. Backlog of open drafts (#202, #204, #209, #210, #211, plus ~30 older log-only PRs) keeps growing.

## IG mirror silently no-op'd — day 35

Row remains `status=fb_published`, `ig_media_id=NULL`, `ig_published_at=NULL`, `error_message=NULL`, `errored_at=NULL`. Same well-documented failure surface as every day since 2026-07-28:

- IG cron `/api/social/ig-mirror` fired at 14:15 UTC. Vercel runtime log counts by statusCode over 14:14–14:20 UTC show `200 × 1` — endpoint returned success, no serverless output.
- `fetchPageContext` at `app/api/social/ig-mirror/route.ts:58-78` throws on the IG-business-account edge because IG account `17841442248205840` is unlinked from FB Page `1086292991223812`; endpoint early-exits at `route.ts:182-192` with `auth/page-context: …` in the response body only — never console-logged, never persisted to the row.
- Backlog is now ~35 rows, well over IG's 25 publishes / 24h rolling ceiling — a re-linked account will need ≥2 throttled days to drain, or the tail can be bulk-marked `ig_failed`.

## Why no new code fix from this session

Per convention established across the last 30+ log entries: 30+ open draft PRs already cover the code repair surface. A 31st competing repair PR fragments review further. The root cause is **upstream of code** — no repo change publishes to an unlinked IG account.

Cleanest ship-ready visibility fix remains **PR #202** (marks pending rows `ig_failed` with the actual Meta error + adds `console.error`).

## Action still required from Kevin (unchanged, now 35 days old)

1. **Meta Business Suite → Business Settings → Accounts → Instagram accounts** — confirm IG `17841442248205840` is linked to FB Page `1086292991223812`; re-link if not.
2. Confirm `META_MARKETING_API_TOKEN` scopes: `instagram_basic`, `instagram_content_publish`, `pages_show_list`.
3. Merge **PR #202** so future outages surface as `ig_failed` with a real `error_message` instead of a silent 200.
4. Decide the fate of the ~35-row backlog: throttled drain (≥2 days) or bulk-mark `ig_failed` with a `stale-backlog` note.
5. Close the remaining 9 competing IG-mirror repair PRs after #202 lands.
6. Merge **PR #211** to break the anecdote monoculture on the FB feed.

## Live-post spot-check

`facebook.com` remains off this session's egress allowlist — direct `WebFetch` on `facebook.com/momfluence/posts` is not available from the routine. `fb_post_id` presence + `status=fb_published` (set only after the Graph API `POST /{page-id}/feed` returns success) is the strongest available signal that the FB post is live.
