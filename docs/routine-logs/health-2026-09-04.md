# Daily content cron health — 2026-09-04

Status: **fb ok / ig silent no-op (day 38)**

## FB row (healthy)
- slug: `gen-2026-09-04-zesot`
- angle_tag: `pumpkin-patch-hayride-line-baby-wipes-rec`
- display: "She named the baby wipes / in the pumpkin patch hayride line."
- fb_post_id: `1086292991223812_122121873302989743` (fb_published_at 14:01:26 UTC)
- fb-daily-generate fired on schedule at 14:00 UTC and inserted-then-published cleanly.

## IG mirror (still failing silently — day 38)
- ig_media_id: `NULL`
- ig_published_at: `NULL`
- error_message: `NULL`, errored_at: `NULL` (nothing persisted — same silent path)
- Row remains stuck at `status='fb_published'` 30+ minutes after the 14:15 UTC cron window.

Same failure mode as days 1–37: `fetchPageContext` throws when the IG Business Account is unlinked from the FB Page, the ig-mirror handler swallows it in its catch and returns `200 { ok: false }` without calling `markFailed`, so no error row surfaces in Supabase and no `console.error` line surfaces in Vercel runtime logs. Diagnosis unchanged from PRs #213 (surface silent ig-mirror failures) and prior — external Meta re-link required from Kevin (IG Business Account → FB Page 1086292991223812), which no code change here can perform.

## PR posture
Log-only, matching the day-30-onward convention. 40+ open draft PRs (#212, #213, #214, #215, …) already cover the code-repair surface (surface silent failures, retry, dead-letter row, etc.); stacking a 41st without upstream re-link would just add noise. This log is the day-38 marker.

## Live-post spot-check
`facebook.com/momfluence/posts` — egress proxy blocks facebook.com (same as prior sessions). Falling back to the Supabase row + Graph-format `fb_post_id` (`<page_id>_<post_id>` well-formed for page 1086292991223812) as proof of publish.

## Action for Kevin
Re-link IG Business Account `17841442248205840` to FB Page `1086292991223812` in Meta Business Suite (Business Settings → Accounts → Instagram accounts → Add / Reconnect, then re-attach to the Page). Once the link is restored, the next 14:15 UTC ig-mirror run should pick up the current `fb_published` row (and, in theory, any prior stuck row the picker still hands it) without a redeploy.
