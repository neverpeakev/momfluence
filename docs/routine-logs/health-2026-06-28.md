# 2026-06-28 daily content cron health

**Status:** degraded — IG mirror failed silently.

**Today's row:** `gen-2026-06-28-cbeer` / angle=`school-cafeteria-lunch-box-thermos-rec` / display="She named the thermos / in the lunch line."
- FB: published 14:02:04 UTC, `fb_post_id=1086292991223812_122115406202989743`
- IG: **not published.** Row stuck at `status='fb_published'`, `ig_media_id=null`, `error_message=null`.

**What happened.** Vercel cron fired `/api/social/ig-mirror` at 14:15:08 UTC (HTTP 200 logged). Supabase API logs confirm the `listPendingIgMirror` SELECT ran at 14:15:08 and returned the row — but no subsequent PATCH on `generated_posts` ever fired. The function exited between the SELECT and any write, most likely via the `fetchPageContext` catch block (line 178-192) which returns 200 with `ok:false` but never marks the row as `ig_failed` and never sets `error_message`. The cron is silently broken: row stays in `fb_published` forever, no error visible in the DB.

**Likely trigger.** Meta `META_MARKETING_API_TOKEN` expired/revoked, or the FB Page → IG Business Account link was severed. Cannot confirm from this session (no env var access). Action: rotate the long-lived Page access token in Vercel env and re-trigger the IG mirror, or wait for tomorrow's cron once the token is refreshed AND the fix below is deployed.

**Fix.** PR on `claude/fix-content-cron-2026-06-28`: when `fetchPageContext` (or env-var check) fails, every pending row is marked `ig_failed` with the underlying error so future runs see the diagnostic and don't silently re-attempt. Return status is also bumped to 502/500 so Vercel logs the failure as an error.

**Spot-check.** FB Page (`facebook.com/momfluence/posts`) and IG profile (`instagram.com/momfluence`) both return 403 to unauthenticated WebFetch (expected — login wall). FB post existence is confirmed via the Graph API `fb_post_id` written to the DB; visual confirmation needs a logged-in browser.

**Notification sent:** yes (push).
