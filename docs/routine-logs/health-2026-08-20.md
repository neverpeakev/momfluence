# Content cron health — 2026-08-20

- **FB post:** OK. Published 14:02 UTC. angle=`scout-meeting-basement-neckerchief-slide-rec`, headline="She named the neckerchief slide / at the scout meeting.", fb_post_id=`1086292991223812_122120577698989743`.
- **IG mirror:** FAILING (long-standing). ig-mirror cron ran 14:15 UTC, returned HTTP 200 with no output. Today's row is stuck at `status=fb_published`, `ig_media_id=NULL`, `error_message=NULL`.
- **Scope:** IG mirroring has never succeeded — 21 consecutive days (2026-07-31 → 2026-08-20), every row `fb_published` with NULL `ig_media_id` and NULL `error_message`.
- **Root cause:** `/api/social/ig-mirror` returned HTTP 200 on all failure paths without logging or persisting, so Vercel counted each run as a success and nothing surfaced. Run exits before the mirror loop (rows never marked `ig_failed`), which points at IG account resolution (`instagram_business_account` edge lookup), not the publish. FB cron used the same token/env 13 min earlier and worked.
- **Fix PR:** https://github.com/neverpeakev/momfluence/pull/200 — makes failure paths loud (log + HTTP 500 + persist `ig_failed`) and resolves the IG account from `META_IG_USER_ID` (=17841442248205840) instead of the fragile Graph edge.
- **Maintainer action:** set `META_IG_USER_ID=17841442248205840` in Vercel prod env; verify token scopes `instagram_basic` + `instagram_content_publish`.
- **Live post spot-check:** SKIPPED — facebook.com is blocked by the network egress proxy in this environment.
- **Backfill:** ~21 FB posts remain un-mirrored; a fixed cron will pick them up (cap 10/run) on next runs.
