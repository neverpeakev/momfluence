# 2026-07-08 content-cron health check

Status: **DEGRADED** — IG mirror silently broken.

- FB post: OK — `gen-2026-07-08-p9hrc` (angle_tag=music-class-shaker-egg-rec) published to FB at 14:01:30 UTC. fb_post_id=1086292991223812_122116418852989743.
- IG mirror: FAILED — row still `fb_published`, no `ig_media_id`, no error_message, cron returned 200.

Systemic issue (not a today-only failure): last successful IG publish was 2026-05-27. Every daily FB post from 2026-06-29 through 2026-07-08 shows `status='fb_published'` with `ig_published_at=null` and `error_message=null`. The immediately-preceding failures (2026-05-23/24/26) were recorded as `ig_failed` with Meta error `code=100, error_subcode=33, "Authorization Error"` — token needs re-authorization / missing scope.

Diagnosis: after ~2026-05-27, the failure moved from inside `publishToIg` (per-row, marked ig_failed with the Meta error) to inside `fetchPageContext` (called once, before the row loop). The catch block returned 200 with `ok:false` in the body but wrote no `console.error` and did not touch any row, so failures became completely invisible to Vercel logs AND to the DB. Almost certainly the same Meta scope/token issue that surfaced in late May, but the code was blind to it.

Fix (PR: TBD — link below after push):
- Add `console.error` at every failure branch in `/api/social/ig-mirror` so future failures show up in Vercel runtime logs.
- Mark the stuck pending rows as `ig_failed` with the Meta error when `fetchPageContext` throws, so they don't accumulate forever and the DB carries the diagnosis.

Action for Kevin: after the PR merges and the next cron runs, check the `error_message` column on the next `ig_failed` row for the exact Meta error. Most likely need to re-issue `META_MARKETING_API_TOKEN` with `instagram_basic` + `instagram_content_publish` scopes. Once fixed, one-off backfill would require unsetting `ig_published_at IS NULL` filter guard — 10 rows are eligible today, IG rate limit is 25/24h.
