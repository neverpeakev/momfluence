# Daily content cron health — 2026-07-25

Status: **degraded** — FB post published, IG mirror silently failing (9-day regression).

## Today's row
- slug: gen-2026-07-25-bf7qz
- angle_tag: holiday-craft-fair-glitter-glue-rec
- display: "She named the glitter glue / at the holiday craft fair."
- fb_post_id: 1086292991223812_122118015230989743 (published 14:01:18 UTC)
- ig_media_id: null, ig_published_at: null, error_message: null
- status: fb_published (should be ig_published)

## Findings
- IG mirror cron at 14:15 UTC returned HTTP 200 (edge-middleware log). No serverless log line, no error_message on the row.
- Pattern is not new: every day from 2026-07-17 through 2026-07-25 shows the same state — status=fb_published, ig never published, error_message null. At least 9 consecutive days silently degraded.
- Because `error_message` is null on every row, the handler is exiting via an early-return path (env-missing, `fetchPageContext` throw, or `pending.length === 0`) that returns 200 without writing to the row. FB works with the same env vars, so the most likely culprit is `fetchPageContext` — probably a missing `instagram_basic` / `instagram_content_publish` scope on `META_MARKETING_API_TOKEN`, or the FB Page → IG Business Account link was broken.

## Action taken
- Opened PR (branch `claude/fix-content-cron-2026-07-25`) that makes the early-exit paths of `ig-mirror` mark every pending row as `ig_failed` with the actual error message, so the next 14:15 UTC run surfaces the real cause on the DB row instead of silently returning 200. This is diagnostic-only — it does not fix the underlying Meta auth/scope issue; that likely needs Kevin to refresh the token with IG scopes or re-link the IG Business Account.
- Did not backfill or retry the 9 stuck rows.
