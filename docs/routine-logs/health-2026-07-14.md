# Daily content cron health — 2026-07-14

Status: **DEGRADED — IG mirror missing**

FB half succeeded; IG half is silently absent. As of the routine run at 14:46 UTC (31 min past the 14:15 UTC ig-mirror schedule), today's row is stuck in `fb_published` with no `ig_published_at`, no `error_message`, and no `errored_at`.

## Row
- id: `9a9652fd-e2d9-4248-86b6-7cff42357531`
- slug: `gen-2026-07-14-nkixc`
- angle_tag: `grocery-checkout-snack-pouch-rec`
- display: "She named the snack pouch / in the checkout line."
- status: `fb_published`
- fb_post_id: `1086292991223812_122117054954989743` (fb_published_at 14:01:16 UTC — healthy)
- ig_media_id: `NULL`
- ig_published_at: `NULL`
- error_message: `NULL`
- errored_at: `NULL`

## Likely root cause
The `fb_published`-with-no-error state is only reachable through one code path in `app/api/social/ig-mirror/route.ts`: `fetchPageContext` throws, the handler returns `{ok:false, message:"auth/page-context: …"}`, and **no row is marked failed** (the try/catch that owns `markFailed` is inside the per-row loop, which never starts). The other reachable "no row change" states (Vercel cron never fires; or the endpoint short-circuits on missing `META_MARKETING_API_TOKEN` / `META_FB_PAGE_ID`) look the same from Postgres.

Same token published to FB 14 min earlier, so a hard token-revoke is unlikely. Two plausible sub-causes:
1. Token has `pages_manage_posts` but is missing `instagram_basic` / `instagram_content_publish` scopes (FB post works, IG lookup fails).
2. Vercel cron for `/api/social/ig-mirror` didn't fire (schedule race / deployment issue).

Cannot distinguish without Vercel runtime logs — Vercel MCP is not connected to this session (only Supabase + GitHub are). Supabase Postgres logs show no `generated_posts` UPDATE traffic in the 14:15 UTC window, which is consistent with either sub-cause.

## Recommended next steps (need human)
1. Open Vercel dashboard → `momfluence-platform` → Logs → filter `/api/social/ig-mirror` around 14:15 UTC. If no invocation appears, it's a cron misfire (retry by hitting the endpoint with the CRON_SECRET). If it appears with `auth/page-context: …`, the token/scope is the problem.
2. To unstick today's row without waiting for tomorrow: `curl -X POST https://momfluence.app/api/social/ig-mirror -H "Authorization: Bearer $CRON_SECRET"` — safe to re-run, `listPendingIgMirror` targets exactly this stuck state.
3. If the run reveals a real code bug (e.g. `fetchPageContext` swallowing a specific error class), fix on `claude/fix-content-cron-2026-07-14` and PR. No PR opened today — root cause is not yet a confirmed code defect.

## Live-post spot-check
`WebFetch https://facebook.com/momfluence/posts` returns 403 (FB blocks unauthenticated scrapes; site homepage also 403s the proxy). Confirming publish via Supabase-recorded `fb_post_id` `1086292991223812_122117054954989743` (well-formed `<page_id>_<post_id>`).

## Access gaps flagged for owner
- No Vercel MCP → cannot inspect cron invocation history or runtime logs. Grant access to close this loop next run.
- Cannot outbound-curl `momfluence.app` (agent proxy returns 403), so cannot self-heal by re-triggering the endpoint.
