# Optimizer tick — 2026-08-23 18:00 UTC

- **Tick ID:** `532bfad8-dfb9-42bf-ac2e-f7eb7e0dc3e1`
- **occurred_at:** 2026-08-23 18:00:59 UTC
- **mode:** shadow
- **duration_ms:** 4242
- **variants_seen:** 10
- **ads_seen:** 45
- **signups_seen:** 2
- **data_pull_ok:** true
- **error:** none

## Decision counts

| decision | count |
| --- | --- |
| no_op | 10 |
| pause | 0 |
| scale | 0 |
| remix | 0 |

Awaiting data — 0 attributed visits across 10 variants, posterior still flat (all p_best ~0.09–0.11, i.e. essentially uniform priors). Every variant hit the same rationale: "Insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)."

## Shadow decisions for review

None — no non-NO_OP actions this tick, so nothing would have been enacted in live mode.

## Note on attribution

Tick-level `signups_seen=2` but per-variant `visits=0` across all 10 variants. Two possibilities:
1. Signups are being counted at the platform level but not yet attributed to a Funnel Lab variant (URL params / referrer not wired through to the attribution join).
2. The 2 signups came from a non-Funnel-Lab source (organic, other campaign) and were correctly excluded from per-variant counts.

Not actionable yet during shadow week, but flagging so it can be checked once real traffic starts flowing — if variant visits stay at 0 while `ads_seen` climbs, it's a plumbing bug, not a data drought.
