# Daily content-cron health — 2026-08-30

**FB path:** healthy. Row `gen-2026-08-30-xzw10` published to Facebook at
14:01:10 UTC. `fb_post_id 1086292991223812_122121440930989743`, angle
`robotics-club-competition-zip-ties-rec`, display "She named the zip ties /
at the robotics competition."

**IG mirror:** silently no-op'd — **day 34 of consecutive outage** (since
2026-07-28). Same failure mode confirmed today at 14:45 UTC:

- `/api/social/ig-mirror` fired at 14:16:01 UTC on prod
  (`dpl_Gvbxxuy1j3CFJGHXUTDCRSWMouVS`, commit `8a63851`), middleware log
  shows `GET 200`, no serverless output.
- Today's row remains `status='fb_published'`, `ig_media_id=NULL`,
  `ig_published_at=NULL`, `error_message=NULL`, `errored_at=NULL`.
- Same silent state on every row 2026-08-25 → 2026-08-30 (last 6 checked;
  the pattern holds all the way back to 2026-07-28 per PR #208).
- Root cause remains: `fetchPageContext` at
  `app/api/social/ig-mirror/route.ts:58-78` throws on the IG-business-
  account edge because IG account `17841442248205840` is unlinked from
  FB Page `1086292991223812`, then the endpoint early-exits at
  `route.ts:182-192` with `auth/page-context: …` in the response body
  only — never console-logged, never persisted to the row.

**Backlog:** ~34 `fb_published` rows now stranded. IG's publish ceiling is
25 per 24h rolling window, so a re-linked account will need 2 throttled
days to drain (or Kevin bulk-marks the tail `ig_failed`).

## No new code fix from this session

30+ open draft PRs already cover this failure surface (see PRs #177, #181,
#187, #191, #193, #195, #197, #199, #200, #202 for the IG silent-failure
repair, plus #178, #179, #182, #184–#186, #189, #190, #192, #194, #201,
#203, #205–#208 for prior day log-only entries). A 31st competing repair
PR would fragment review further. Recommended visibility fix to ship:
**PR #202** (marks pending rows `ig_failed` with the actual Meta error and
adds `console.error`; the cleanest of the ten IG-mirror repair PRs).

## Action required from Kevin — unchanged and now 34 days old

1. **Meta Business Suite → Business Settings → Accounts → Instagram
   accounts** — confirm IG `17841442248205840` is linked to FB Page
   `1086292991223812`. Re-link if not.
2. Confirm `META_MARKETING_API_TOKEN` scopes still include
   `instagram_basic`, `instagram_content_publish`, `pages_show_list`.
3. Merge **PR #202** so any future outage surfaces as `ig_failed` with a
   real `error_message` instead of a silent 200.
4. Decide the fate of the ~34-row backlog: throttled drain (≥2 days at
   IG's 25/24h ceiling) or bulk-mark `ig_failed` with a `stale-backlog`
   note (mirror the 2026-07-29 remediation approach).
5. Close the remaining 9 competing IG-mirror repair PRs after #202 lands.
