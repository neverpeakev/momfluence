# Daily content cron — 2026-06-24

**Status: DEGRADED — IG mirror still silently failing. No new diagnosis; the existing open PRs cover it.**

## Today's row

- `id`: `eef24e2a-cec5-48e1-9f2d-c910f0c3d1b1`
- `slug`: `gen-2026-06-24-6bklg`
- `angle_tag`: `preschool-pickup-rain-boots-rec`
- `display`: "She named the rain boots / at the puddle by the gate."
- `status`: `fb_published` (expected `ig_published`)
- `fb_post_id`: `1086292991223812_122115026378989743` (FB publish at 14:01:10 UTC — healthy)
- `ig_media_id`: NULL
- `ig_published_at`: NULL
- `error_message`: NULL
- `errored_at`: NULL

## Vercel runtime log for `/api/social/ig-mirror`

```
14:15:08  GET /api/social/ig-mirror  200  dep=dpl_Gvbxxuy1j3CFJGHXUTDCRSWMouVS
```

Single access-log line, no stdout/stderr — same silent-200 pattern documented in PR #133 and PR #134. The 14:15 UTC cron fired, auth passed, the handler returned 200, and no DB write happened.

## How long this has been broken

Same failure mode every day since at least 2026-06-18 (7 days observed in this query; PR #134 traces it back to 2026-05-27 — ~28 days). Every row sits at `fb_published` with `ig_media_id NULL` and a NULL `error_message`.

```
2026-06-24  fb_published  ig_published_at=NULL  error_message=NULL
2026-06-23  fb_published  ig_published_at=NULL  error_message=NULL
2026-06-22  fb_published  ig_published_at=NULL  error_message=NULL
2026-06-21  fb_published  ig_published_at=NULL  error_message=NULL
2026-06-20  fb_published  ig_published_at=NULL  error_message=NULL
2026-06-19  fb_published  ig_published_at=NULL  error_message=NULL
2026-06-18  fb_published  ig_published_at=NULL  error_message=NULL
```

## Why I did NOT open a new PR today

Two open draft PRs already exist for this exact issue:

- **PR #133** (`claude/fix-content-cron-2026-06-22`) — adds `console.error` to the three silent-200 paths in `app/api/social/ig-mirror/route.ts` so the next run leaves a usable trace.
- **PR #134** (`claude/fix-content-cron-2026-06-23`) — supersedes #133. Adds the logging AND converts systemic failures (env-missing, `fetchPageContext` throw) into HTTP 500s + writes per-row `markFailed` so the failure is visible in the DB, Vercel logs, and the cron scheduler. Identifies root cause as external Meta-side: `error_subcode 33` (`GraphMethodException`) on the IG side, while the same token still publishes FB fine — implying either the IG-Business-Account link to FB Page `1086292991223812` was broken in Business Settings, or the token's `instagram_basic` / `instagram_content_publish` scopes were dropped.

Opening a third PR would be noise. The blocker is operator action, not code.

## Action required (operator — out of scope for this routine)

1. Merge **PR #134** (#133 supersedes-by-#134; close or ignore #133).
2. In Meta Business Settings → Pages → Momfluence (`1086292991223812`) → Instagram accounts: confirm `@momfluence` (`17841442248205840`) is still linked. Re-link if not.
3. Business Settings → System Users → owner of `META_MARKETING_API_TOKEN` → Assets: confirm FB Page + IG account assigned, scopes still include `instagram_basic`, `instagram_content_publish`, `pages_show_list`, `pages_read_engagement`, `pages_manage_posts`, `business_management`.
4. If scopes were dropped, generate a new long-lived system-user token and update Vercel env `META_MARKETING_API_TOKEN` for production; redeploy.
5. Once auth is fixed, `listPendingIgMirror` (cap 10) will drain the ~7-row backlog over a single day, well under IG's 25-per-24h limit.

## Verification of FB publish

WebFetch on `facebook.com/momfluence/posts` returns 403 (FB blocks anonymous scraping). The FB publish is corroborated by the `fb_post_id` returned by Meta's `/photos` endpoint and the `markFbPublished` write — both indicators consistent with a real publish. Operator can spot-check at https://www.facebook.com/momfluence by visiting the Page.

## Links

- PR #133 — https://github.com/neverpeakev/momfluence/pull/133
- PR #134 — https://github.com/neverpeakev/momfluence/pull/134
- Prior log — `docs/routine-logs/health-2026-05-13.md` (only one before today; the routine wasn't running daily until now)
