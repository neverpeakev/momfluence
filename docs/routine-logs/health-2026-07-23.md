# Daily content-cron health — 2026-07-23

## Verdict

**Partial. FB healthy, IG silent-failing for the 56th consecutive day.**

Today's row (id `8ef4777d-09e8-48c5-b785-727efc64a94b`):

| field              | value                                          |
|--------------------|------------------------------------------------|
| slug               | `gen-2026-07-23-6vh99`                         |
| angle_tag          | `science-fair-poster-board-glue-rec`           |
| display            | "She named the glue sticks / at the science fair." |
| status             | `fb_published`                                 |
| fb_post_id         | `1086292991223812_122117837174989743`          |
| fb_published_at    | `2026-07-23 14:01:12 UTC`                      |
| ig_media_id        | `null`                                         |
| ig_published_at    | `null`                                         |
| error_message      | `null`                                         |

The last 9 days show the same pattern in `generated_posts`: one row per day,
`fb_published`, no IG side, no `error_message`. The 2026-07-16 row is missing
entirely (separate gap; not investigated this cycle).

## Not opening PR #168

Nine consecutive `claude/fix-content-cron-*` PRs are already open and
unmerged against `main` addressing this exact silent-IG-failure. Every one
adds the same observability shim to `/api/social/ig-mirror`:

- #151 (2026-07-08), #153 (2026-07-10), #159 (2026-07-15),
  #161 (2026-07-17), #162 (2026-07-18), #163 (2026-07-19),
  #166 (2026-07-21), #167 (2026-07-22)

None have merged. PR #167 (yesterday) has the most thorough diagnosis and
diff — it's the one to review. Opening a tenth PR today would add zero
signal and more noise. Following the same call the 2026-07-20 supervisor
already made (see commit `29cca841`).

## Root cause (from PR #167 diagnosis, not re-derived here)

Almost certainly `META_MARKETING_API_TOKEN` is missing `instagram_basic` +
`instagram_content_publish` scopes. `fetchPageContext` throws
`"No Instagram Business Account linked to FB Page 1086292991223812"` before
any row is touched, and the endpoint's early-return path never calls
`markFailed`, so `error_message` stays null. The reason today's row *still*
shows `error_message=null` — even after multiple PRs written to surface it —
is that none of those PRs have deployed.

## Action needed from owner

1. Merge PR #167 (or any one of the stack) so tomorrow's 14:15 UTC run
   leaves a diagnosable error string on the row.
2. Re-issue `META_MARKETING_API_TOKEN` in Vercel env with
   `instagram_basic` + `instagram_content_publish` +
   `pages_show_list` + `pages_manage_posts` scopes.
3. Confirm FB Page `1086292991223812` still shows IG Business Account
   `17841442248205840` linked in Meta Business Suite.
4. After the next run, drain the 56-row backlog manually (raise the
   `listPendingIgMirror` cap and trigger `/api/social/ig-mirror` once)
   or let it drain naturally at 10/day.

## FB post spot-check

Not verified via WebFetch — Facebook returns 403 to unauthenticated
scrapers. `fb_post_id` on the row is authoritative; permalink is
`https://www.facebook.com/1086292991223812/posts/122117837174989743`.
