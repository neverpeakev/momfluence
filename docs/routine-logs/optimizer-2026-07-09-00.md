# Optimizer supervisor log — 2026-07-09 00:00 UTC tick

## Tick metadata

- **Tick ID:** `600d55ed-4eca-4c1d-906d-c1f7166e3293`
- **occurred_at:** 2026-07-09 00:01:04 UTC (within 8h window — cron healthy)
- **Mode:** `shadow` (week-1 observation)
- **duration_ms:** 2,388
- **variants_seen:** 10
- **ads_seen:** 45
- **signups_seen:** 2
- **data_pull_ok:** true
- **error_message:** none

## Decision counts

| Decision | Count |
|---|---|
| PAUSE  | 0 |
| SCALE  | 0 |
| REMIX  | 0 |
| NO-OP  | 10 |

All 10 variants NO-OP'd with the same rationale: `Insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)`. Per-arm posterior remains flat (`p_best` ~0.096–0.107, `p_worst` ~0.090–0.104 — essentially the uniform prior across 10 arms).

## Shadow decisions for review

None. No non-NO-OP actions this tick, so nothing would have fired in live mode.

## Awaiting data

Posterior is still flat — no arm has crossed the `min_visits_per_arm=100` / `min_conversions_per_arm=5` gates yet. 0 visits and 0 conversions recorded against every variant.

## ⚠ Attribution gap worth watching

Tick metadata says `ads_seen=45` and `signups_seen=2`, but every per-arm rollup shows `visits=0`, `conversions=0`, `spend_usd=0`. Meta-level pulls appear to be landing but aren't being attributed to variants in the per-arm table. If the next few ticks continue to show tick-level `signups_seen > 0` while per-arm `conversions=0`, the variant-attribution join needs a look (likely `visits`/`ad_events` → `variant_slug` mapping).

Not raising as a bug yet — could just be day-1 data still propagating. Flag if it persists past 2026-07-10.

## Settings snapshot

- `mode` = shadow
- `min_visits_per_arm` = 100
- `min_conversions_per_arm` = 5
- `p_best_threshold` = 0.80
- `p_worst_threshold` = 0.95
- `max_pauses_per_tick` = 1
- `max_scales_per_tick` = 1
- `scale_pct_per_day` = 0.30
- `max_budget_multiplier` = 3.0
- `starting_daily_budget` = $30
