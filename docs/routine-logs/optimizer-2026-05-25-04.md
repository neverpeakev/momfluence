# Optimizer tick log — 2026-05-25 04:00 UTC

## Tick

- **Tick ID:** `0d17c895-71d0-4a28-9a0d-cfd535784279`
- **Occurred at:** 2026-05-25 00:00:41 UTC (≈4h before this check — cron healthy)
- **Mode:** shadow
- **Duration:** 1658 ms
- **Variants seen:** 10
- **Ads seen:** 45
- **Signups seen:** 1
- **Data pull OK:** true

## Decision counts

| Decision | Count |
|----------|-------|
| NO-OP    | 10    |
| PAUSE    | 0     |
| SCALE    | 0     |
| REMIX    | 0     |

## Summary

Awaiting data — 0 visits across 10 variants, posterior still flat (p_best ≈ p_worst ≈ 0.10 for every variant, i.e. a uniform 1/N prior). Every variant tripped the same insufficient-data guard (`visits < 100`, `conv < 5`), so the optimizer correctly held with no actions.

One signup was recorded at the funnel level (`signups_seen=1`) but did not attach to any variant's visit counter, so it didn't move any posterior. Worth a glance next tick if visits still read zero — could indicate the per-variant attribution write isn't landing — but one tick of zero visits on day 1 of week-1 observation is expected.

No supervisor action required. Shadow mode unchanged.
