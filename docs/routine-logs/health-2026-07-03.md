# Daily content cron health — 2026-07-03

Status: **fail (fixed forward)**

FB cron fired at 14:01:41 UTC and threw at stage=`generate`. No row was inserted into `generated_posts` today (query returned zero rows). IG mirror at 14:15:08 UTC found nothing pending and no-op'd.

## Failure

`GET /api/social/fb-daily-generate 200` — `generateDailyPost failed after 3 attempts. Last error: blocklist hit: "here's the thing".`

Last raw response (attempt 3, truncated in Vercel log):
- angle_tag: `pediatrician-waiting-room-teething-toy-rec`
- content_format: `anecdote`
- display: `She named the teething toy / in the pediatrician's waiting room.`

The caption contained `here's the thing` and hit the code-side blocklist. All 3 retries did the same.

## Root cause

Prompt/code drift. `lib/social/post-generator.ts` has `here's the thing` in the runtime BLOCKLIST array (line 99) but the `DEAD PHRASES (auto-reject on use)` section of the SYSTEM_PROMPT sent to Claude only lists the sibling overused openers (`"Real talk," "hot take," "let's be honest"`) — not `here's the thing`. Claude has no signal to avoid it up front, and the retry note (`Previous attempt failed: blocklist hit: "here's the thing"`) is not strong enough guidance against a phrase the system prompt still implicitly permits. Three attempts in a row landed on it.

## Fix

PR: https://github.com/neverpeakev/momfluence/pull/<TBD> (branch `claude/fix-content-cron-2026-07-03`)

`lib/social/post-generator.ts` DEAD PHRASES bullet — add `here's the thing` to the overused-openers line so Claude sees the ban in the system prompt, not just after retry.

## Follow-up

- No row was written, so nothing to backfill in `generated_posts`. Today's FB post is missing.
- Optional: after this PR merges, manually trigger `POST /api/social/fb-daily-generate` with `Authorization: Bearer $CRON_SECRET` to publish a same-day post, then wait for the 14:15 IG mirror tomorrow (or trigger `/api/social/ig-mirror` manually).
- Consider a general prompt/blocklist parity check (each BLOCKLIST entry should have a matching DEAD PHRASES bullet). Not in scope for today's hotfix.
