# Daily content cron health — 2026-05-12

**Status:** FAILED (no row for today; cron did not run)

## Diagnosis

- Supabase `generated_posts` returned 0 rows for `created_at >= date_trunc('day', now())`.
- Vercel runtime logs for project `momfluence-platform` (prj_FpnpeQiY122gZaviqT1XlqtZJeom) show no invocations of `/api/social/fb-daily-generate` or `/api/social/ig-mirror` in the last 48h.
- Current production deployment is `dpl_DUxd1nNQELmfuukz6hyY1WVS6eVC` at sha `979f04b1` — the PR #22 merge ("Drop company_overview from page metadata POST"). This is the commit just *before* the social-content-cron work.
- PR #23 ("Add daily content cron — Claude generates new FB posts, IG mirror picks up", branch `claude/social-content-cron`, sha `0deb474`) is **open and unmerged** (`mergeable_state: clean`). It introduces both API route handlers (`app/api/social/fb-daily-generate/route.ts`, `app/api/social/ig-mirror/route.ts`) and the `vercel.json` cron schedule entries that bind 14:00 UTC and 14:15 UTC to those routes.
- Because the routes and the cron schedule both live only on the unmerged branch, Vercel's cron scheduler has nothing to invoke in production. The previously-deployed preview (`dpl_Hh3naVnsm33PpQws8rP9oGRnDDXE`, target=null) does contain the code, but preview deployments do not receive scheduled cron invocations.

## Root cause

PR #23 was never merged to `main`. The daily content cron has not been live in production at any point yet, so today's missed run is not a regression — it's the absence of a deploy.

## Action taken

None automated. Not opening a new PR — the change already exists as PR #23 and is mergeable. Not merging #23 unilaterally (state-changing, requires user authorization, and the PR's own test plan asks for a manual `curl` verification post-deploy).

## Recommended next step for operator

1. Review and merge https://github.com/neverpeakev/momfluence/pull/23.
2. After production deploy finishes, manually fire the FB cron once to seed today's row:
   ```
   curl -X POST https://momfluence.app/api/social/fb-daily-generate \
     -H "Authorization: Bearer $CRON_SECRET"
   ```
3. ~30s later, fire the IG mirror:
   ```
   curl -X POST https://momfluence.app/api/social/ig-mirror \
     -H "Authorization: Bearer $CRON_SECRET"
   ```
4. From tomorrow on, the 14:00 / 14:15 UTC crons will fire automatically.

## Notes

- FB Page spot-check (`https://facebook.com/momfluence/posts`) returned HTTP 403 to unauthenticated WebFetch. Cannot confirm scheduled seed posts from PR #20 published as expected via this tool; operator can verify directly in the Page.
- Time budget: ~4 min used.
