# Optimizer tick — 2026-07-18 18:00 UTC

## Tick

- **Tick ID:** `0ae8aa91-08bd-494c-9367-50db76996473`
- **Occurred at:** 2026-07-18 18:00:09 UTC (4h before this supervisor run — cron healthy)
- **Mode:** `shadow` (week 1 observation)
- **Duration:** 9,051 ms
- **Variants seen:** 10
- **Ads seen:** 45
- **Signups seen:** 2
- **Data pull ok:** yes; no error

## Decisions

| Type   | Count |
|--------|------:|
| PAUSE  | 0 |
| SCALE  | 0 |
| REMIX  | 0 |
| NO-OP  | 10 |

All 10 variants triggered NO-OP with rationale: *"Insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)"*. Posterior p_best / p_worst are still clustered around the uniform 0.10 prior (range 0.0957–0.1070), which is the expected shape when the model has no evidence to update on.

**Awaiting data — 0 visits across all 10 variants; posterior still flat.** No shadow decisions to translate.

## Signal to watch

`signups_seen = 2` at the tick level while every per-variant row shows `visits = 0` and `conversions = 0`. Those 2 signups therefore aren't being attributed to any variant. Likely explanations: signups came from non-ad traffic, or the click→variant attribution join isn't wiring visits into the per-variant rollup yet. Worth confirming before we exit the shadow observation period — if we flip to live with attribution broken, PAUSE/SCALE decisions would be based on `visits=0` for every variant and the optimizer would never act.

If the next 1–2 ticks also show variant `visits=0` while tick-level `signups_seen > 0`, escalate — this is a pipeline bug, not a data-volume issue.
