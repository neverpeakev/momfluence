# Optimizer tick — 2026-07-17 06:00 UTC

## Run health
- **Tick ID:** `363d0bf9-9515-4130-b124-3ec495316fc2`
- **Occurred at:** 2026-07-17 06:01:01 UTC
- **Mode:** `shadow`
- **Duration:** 1,899 ms
- **Variants seen:** 10
- **Ads seen:** 45
- **Signups seen:** 2
- **Data pull:** ok, no error

Cron fired on schedule; ingest healthy.

## Decisions by type

| Decision | Count |
| --- | --- |
| NO-OP | 10 |
| PAUSE | 0 |
| SCALE | 0 |
| REMIX | 0 |

## Detail

All 10 variants returned `no_op` with the same rationale: *"Insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)"*. Per-variant posterior probabilities (`p_best`/`p_worst`) are all clustered tightly around ~0.10, i.e. a flat, uninformative prior — the Bayesian model has nothing to update on yet at the per-variant level.

Note the discrepancy between tick-level counters (ads_seen=45, signups_seen=2) and the per-action `visits=0` for every variant. That means the attribution join used by `optimizer_actions` isn't yet linking site visits back to the ad variants — either the click→visit pixel mapping is still cold, or the attribution window in the action query hasn't caught the first 2 signups. Worth checking once we cross the ≥100-visit threshold on at least one variant; not actionable this tick.

## Shadow decisions for review

None — no non-NO-OP decisions were produced, so there's nothing that *would have* been enacted in live mode this tick.

## Status

Awaiting data — 0 attributed visits across 10 variants, posterior still flat. Continuing shadow observation.
