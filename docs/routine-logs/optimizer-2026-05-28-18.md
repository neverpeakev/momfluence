# Optimizer supervision — 2026-05-28 18:00 tick

Status: **healthy — awaiting data (all NO-OP)**

Cron ran on schedule (~4h before this review at 22:08 UTC). No action needed.

## Tick
- Tick ID: `74c049d3-2302-4bfd-ac0d-f6d7ac7f8edf`
- occurred_at: 2026-05-28 18:00:34 UTC
- mode: **shadow** (confirmed against `optimizer_settings`, set 2026-05-11)
- duration_ms: 2882
- variants_seen: 10
- ads_seen: 45
- signups_seen: 1
- data_pull_ok: true (error_message: none)

## Decisions by type
- NO-OP: 10
- PAUSE: 0
- SCALE: 0
- REMIX: 0

No non-NO-OP decisions, so there is nothing that would have been enacted in live mode this tick.

## Awaiting data
0 visits across all 10 variants, 0 conversions. Posterior is still flat — `p_best ≈ p_worst ≈ 0.10` for every variant, i.e. the uniform 1/10 prior is untouched. Every action's rationale is the same gate: "Insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)." No variant is anywhere near the decision thresholds yet.

| variant | creative | p_best | p_worst | visits | conv |
|---|---|---|---|---|---|
| twenty-five-day-one | c18 | 0.1070 | 0.1011 | 0 | 0 |
| move-over-influencers | c17 | 0.1023 | 0.1025 | 0 | 0 |
| brand-wall | c16 | 0.1021 | 0.1040 | 0 | 0 |
| heads-up-moms | c13 | 0.1009 | 0.1011 | 0 | 0 |
| faceless-creator | c20 | 0.1000 | 0.0965 | 0 | 0 |
| chatgpt-writes-it | c15 | 0.0994 | 0.1023 | 0 | 0 |
| no-influencer-needed | c12 | 0.0983 | 0.0905 | 0 | 0 |
| group-chat-goldmine | c11 | 0.0977 | 0.0999 | 0 | 0 |
| you-already-do-this | c14 | 0.0966 | 0.1013 | 0 | 0 |
| real-receipts | c19 | 0.0957 | 0.1008 | 0 | 0 |

## Flag for Kevin (not a cron fault)
The tick reports `ads_seen=45` and `signups_seen=1`, but per-variant `visits` and `conversions` are all 0. So traffic/conversions are not flowing through to the variant level the optimizer scores on — either variant attribution isn't wired up yet, or the ads only just went live and haven't accrued attributed visits. `data_pull_ok=true` and the cron itself is healthy, so this is a funnel-attribution question, not an optimizer bug. Worth a look before the shadow→live decision, since the optimizer cannot leave NO-OP until it sees ≥100 visits / ≥5 conv per variant.

## Supervisor note (runbook drift)
The runbook's column names are stale vs. the live schema. Mapping used here:
- `decision_type` → `action_type`
- `ad_id` → `meta_ad_id`
- `variant_slug` → `variant`
- no `sample_size` column — used `visits` (with `conversions`) as the sample-size signal
- `optimizer_settings` is a key-value store (`id`/`value`), so mode = row where `id='mode'`, not a `mode` column

Worth updating `docs/funnel-lab.md` / the runbook so future reviews don't re-derive this.
