# Daily content cron health — 2026-07-09

Status: **fb_published, IG mirror silently broken (44th day in a row)**

Today's FB post published cleanly at 14:02:08 UTC. IG mirror cron fired at 14:15:08 UTC, returned 200, and did nothing observable — no `ig_published_at`, no `error_message`, no `console.error` in Vercel runtime logs. Same failure mode as every day since 2026-05-27.

## Today's row
- slug: `gen-2026-07-09-f455k`
- angle_tag: `gymnastics-viewing-window-leotard-rec`
- display: "She named the leotard / through the viewing window."
- status: `fb_published`
- fb_post_id: `1086292991223812_122116529234989743` (fb_published_at 14:02:08 UTC)
- ig_media_id: `null`
- error_message: `null`

## Why the fix isn't live yet
PR #151 (https://github.com/neverpeakev/momfluence/pull/151, branch `claude/fix-content-cron-2026-07-08`) diagnoses this exact scenario and adds:
- `console.error` at the `fetchPageContext` catch so Vercel surfaces the Meta error body.
- `markFailed(row.id, "ig", msg)` for every pending row when `fetchPageContext` throws, so the DB stops silently re-queuing indefinitely.

The PR is still `state=open, draft=true, merged=false` a day after being opened. Nothing today has been re-diagnosed — the fix was correct, it just hasn't been merged.

Ig-mirror runtime log excerpt (14:15:08 UTC):
```
GET /api/social/ig-mirror 200 [info/edge-middleware]
  dep=dpl_Gvbxxuy1j3CFJGHXUTDCRSWMouVS branch=main cache=BYPASS
```
No stderr. No serverless-function stdout. Consistent with `fetchPageContext` throwing before the per-row loop and the current catch block returning `{ok:false}` without logging or writing to the DB.

## Backlog
`listPendingIgMirror` currently returns 43 rows dating back to 2026-05-29 (query at 14:46 UTC on 2026-07-09). Once PR #151 lands and the next cron fires, all of them will flip to `ig_failed` with populated `error_message` — that's expected, not new damage.

## Operator action
1. Merge PR #151 (mark ready, review, merge to main).
2. Wait for 14:15 UTC 2026-07-10 cron, read the `error_message` on the newly-`ig_failed` rows to confirm the Meta failure (expected: authorization / missing scope).
3. Re-issue `META_MARKETING_API_TOKEN` with `instagram_basic` + `instagram_content_publish` scopes.
4. Optional backfill: reset the 43 stuck rows' `status` to `fb_published` (clearing `error_message`) so `listPendingIgMirror` picks them up again. Rate limit is 25 IG media publishes / 24h per account — the backfill should be paced across 2 days.

## Live-post spot-check
WebFetch of `facebook.com/momfluence/posts` → 403 (unauth). Relying on Supabase's `fb_post_id` (well-formed `<page_id>_<post_id>`) as proof of FB publish; IG side is confirmed *not* live via the DB (`ig_media_id=null`).
