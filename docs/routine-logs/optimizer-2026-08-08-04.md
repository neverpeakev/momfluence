# Optimizer supervision — 2026-08-08 04:00 UTC

Fires 4h after the 00:00 UTC cron tick.

## Tick
- **Tick ID:** `a261fbf6-80cf-4e36-a1c4-8a08747f0a58`
- **occurred_at:** 2026-08-08 00:01:00 UTC (≈4h ago — cron healthy)
- **Mode:** `shadow`
- **duration_ms:** 1552
- **variants_seen:** 10
- **ads_seen:** 45
- **signups_seen:** 2
- **data_pull_ok:** true

## Decision counts
| decision | n |
|---|---|
| no_op | 10 |

## Summary
Awaiting data — 0 attributed visits across 10 variants; posterior still flat (p_best/p_worst all near 0.10, i.e. uniform prior). Every action is `no_op` with rationale *"Insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)"*. No shadow decisions to review.

Note: the tick recorded `signups_seen=2` at the aggregate level, but per-variant `visits=0` for all 10. Either signups aren't being attributed to variants yet, or the variant-level visit rollup is not populating. Worth watching over the next several ticks; if signups continue to arrive without per-variant attribution, the optimizer will never leave `no_op` regardless of traffic. Not raising as a fix now — could be normal wiring delay on day 1 of the observation window.

## Health
- Cron: ✅ on schedule
- Data pull: ✅
- Errors: none
