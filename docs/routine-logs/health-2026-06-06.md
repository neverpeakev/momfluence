# Daily content cron health — 2026-06-06

**Status:** DEGRADED. 9th consecutive day of silent `ig-mirror` failure.

## Today's run

- `/api/social/fb-daily-generate` (14:00 UTC) → 200 OK. Row inserted, FB published.
  - slug `gen-2026-06-06-aj8dm`, angle `target-dressing-room-jeans-rec`
  - display "She told the mom next door / which jeans to grab."
  - `fb_post_id` `1086292991223812_122113268354989743` at 14:01:10 UTC
- `/api/social/ig-mirror` (14:15 UTC) → 200 OK in <1s. No render pre-warm hit
  (no `/api/render/post/...` log around 14:15), no `console.error`, DB row
  unchanged. Same silent-early-return shape as the previous 8 days.

## Backlog (rows stuck in `status='fb_published'`, no `error_message`)

| slug | fb_published_at (UTC) |
| --- | --- |
| gen-2026-05-29-kvrzn | 2026-05-29 14:01:14 |
| gen-2026-05-31-zimk2 | 2026-05-31 14:01:07 |
| gen-2026-06-02-btc5f | 2026-06-02 14:01:13 |
| gen-2026-06-03-11y09 | 2026-06-03 14:01:17 |
| gen-2026-06-04-su3dq | 2026-06-04 14:01:24 |
| gen-2026-06-05-40egu | 2026-06-05 14:02:04 |
| gen-2026-06-06-aj8dm | 2026-06-06 14:01:10 |

7 FB posts now live on the Page without their IG counterpart. Last successful
IG publish: `gen-2026-05-27-p8u57` (10 days ago).

The older `ig_failed` rows (`gen-2026-05-23/24/26`) carry the explanatory
Meta error: `code:100, type:GraphMethodException, error_subcode:33`
("Authorization Error" — token lacks the requested permissions). Strongly
suggests `META_MARKETING_API_TOKEN` lost `instagram_basic` /
`instagram_content_publish` scopes when last rotated.

## No new PR opened today

The fix is already on the table and has been for 2 days:

- **PR #112** (`claude/fix-content-cron-2026-06-05`, opened 2026-06-05) —
  canonical fix. Adds `console.error` on every silent return so the failure
  mode finally surfaces, plus a `META_IG_BUSINESS_ID` env override that
  bypasses the broken `instagram_business_account` lookup. Setting
  `META_IG_BUSINESS_ID=17841442248205840` after merge would unstick all 7
  backlog rows on a single manual cron trigger.
- **PR #111** (`claude/fix-content-cron-2026-06-04`, opened 2026-06-04) —
  narrower companion: marks pending rows `ig_failed` with the actual Meta
  error when `fetchPageContext` throws, so future failures aren't silent.

Both are open, draft, mergeable=clean. Opening a 3rd PR with the same code
won't help. Posting an escalation comment on #112 instead.

## Action required from @kevin

1. **Merge PR #112** (and ideally #111 for defense-in-depth).
2. Set `META_IG_BUSINESS_ID=17841442248205840` in Vercel production env.
3. Manually trigger `POST /api/social/ig-mirror` with `Authorization: Bearer
   $CRON_SECRET` — should return `mirrored: 7`.
4. Separately, re-grant `instagram_basic` + `instagram_content_publish` on
   `META_MARKETING_API_TOKEN` in Meta Business Settings so the lookup path
   works without the env override.

Until (1) and (2) happen, every 14:15 UTC cron will add another row to the
backlog and emit zero diagnostic output.
