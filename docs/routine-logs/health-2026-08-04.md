# Daily content cron health — 2026-08-04

Status: **degraded (IG mirror silent no-op, day 7+ of same known issue)**

FB post generated and published cleanly. IG mirror ran on schedule (200) but again produced no application logs and left today's row stuck at `fb_published`. No new root cause — this is the same operational blocker documented in every routine log since 2026-07-29.

## Today's row
- slug: `gen-2026-08-04-af79r`
- angle_tag: `field-trip-permission-slip-clipboard-rec`
- display: "She named the clipboard / at field trip check-in."
- status: `fb_published`
- fb_post_id: `1086292991223812_122119012616989743` (fb_published_at 14:02:04 UTC)
- ig_media_id: `null`
- ig_published_at: `null`
- error_message: `null` (silent skip, as expected on this deployment)
- errored_at: `null`

## Cron activity (production, via Vercel runtime logs)
- 14:01:35 UTC — `GET /api/social/fb-daily-generate` → 200 ✓
- 14:15:08 UTC — `GET /api/social/ig-mirror` → 200 (zero `console.*` output from handler)

Both fired on schedule against production deployment `dpl_Gvbxxuy1j3CFJGHXUTDCRSWMouVS` (commit `8a63851`, "Fix embedded checkout: ui_mode embedded_page (#108)"). That deployment predates PR #181's ig-mirror logging changes, so the silent-skip pattern remains unchanged in production.

## Root cause (unchanged since ~2026-05-28)
`app/api/social/ig-mirror/route.ts` returns JSON on three "expected" failure paths (`!userToken || !pageId`, `pending.length === 0`, `fetchPageContext` throws) with no `console.*` output. Vercel cron ignores response bodies, so those paths are invisible in logs.

Row exists and matches `listPendingIgMirror`, so `pending.length === 0` is ruled out. Env vars just worked at 14:02 in `/api/social/fb-daily-generate`, so the missing-env branch is ruled out. Only remaining consistent path is `fetchPageContext` throwing — most likely because the FB Page's `instagram_business_account` field is unlinked / the system-user token lacks `instagram_basic` + `instagram_content_publish` for IG business account `17841442248205840`.

Not a code bug — an operational Meta-side state that requires re-linking the IG Business Account in Meta Business Suite, or refreshing the system-user token scopes.

## Existing fix in flight
- PR #181 (`claude/fix-content-cron-2026-08-03`, opened 2026-08-03 14:54 UTC, **still draft, still unmerged**) already adds the three log lines needed to make this diagnosable next time it recurs. Not deploying it is why today's ig-mirror produced no logs.

No new PR opened — duplicating #181 adds noise. Today's contribution is this log entry so the pattern's day-count is visible.

## Backlog of stuck rows
Every day since ~2026-07-29 has added a fresh `fb_published` row with null `error_message`. Not measured this run (out of time budget), but per the 2026-08-02 log the count was 6 rows as of that day; today makes at least 8. Rows are recoverable via a manual POST to `/api/social/ig-mirror` with `Authorization: Bearer $CRON_SECRET` **once the underlying Meta link is restored**.

## Live-post spot-check
WebFetch of `facebook.com/momfluence/posts` returns 403 (Meta blocks unauthenticated scrapes). Verified via the well-formed Graph-API `fb_post_id` in Supabase — same proof-of-publish approach as prior logs.

## Recommended next action (for Kevin, out of session scope)
1. Merge PR #181 so the observability fix reaches production — tomorrow's cron will then log the actual Meta API error.
2. In Meta Business Suite, verify the FB Page → IG Business Account link (page `1086292991223812` → IG `17841442248205840`) and re-link if needed.
3. After both are in place, POST to `/api/social/ig-mirror` with the cron bearer to drain the backlog.
