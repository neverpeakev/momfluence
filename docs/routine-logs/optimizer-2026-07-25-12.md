# Optimizer tick — 2026-07-25 12:00 UTC

## Run summary

- **Tick ID**: `17476522-6e2a-4ed1-9543-7e7071084177`
- **Occurred at**: 2026-07-25 12:00:09 UTC
- **Mode**: `shadow`
- **Duration**: 1,767 ms
- **Variants seen**: 10
- **Ads seen**: 45
- **Signups seen**: 2
- **Data pull**: ok
- **Error**: none

## Decisions

| decision | count |
| --- | ---: |
| NO-OP | 10 |
| PAUSE | 0 |
| SCALE | 0 |
| REMIX | 0 |

All 10 variants returned NO-OP with the same rationale:
`Insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)`

Posterior probabilities are effectively flat across all arms (`p_best` ≈ 0.10, `p_worst` ≈ 0.10), which is expected before any per-variant visit attribution accumulates.

## Interpretation

Awaiting data — 0 attributed visits across all 10 variants, posterior still flat. The tick did register `signups_seen = 2` at the run level, but none carried variant attribution yet, so no arm crossed the `min_visits_per_arm = 100` / `min_conversions_per_arm = 5` threshold. Nothing for a human to review this cycle.

Consistent with the week-1 shadow observation posture; no action needed.

## Settings snapshot

- `mode = shadow`
- `min_visits_per_arm = 100`
- `min_conversions_per_arm = 5`
- `p_best_threshold = 0.80`
- `p_worst_threshold = 0.95`
- `max_pauses_per_tick = 1`
- `max_scales_per_tick = 1`
- `scale_pct_per_day = 0.30`
- `max_budget_multiplier = 3.0`
- `starting_daily_budget = 30`
