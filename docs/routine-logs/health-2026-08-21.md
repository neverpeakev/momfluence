# Daily content cron health — 2026-08-21

Status: **fb-only (IG mirror persistently broken since ~2026-05-28)**

FB publish succeeded. IG mirror ran, returned 200, but did not publish and did not mark the row `ig_failed` — the Instagram Business Account is still unlinked from the FB Page, so `fetchPageContext` throws before the per-row loop and the row stays `fb_published` forever. Today's row is the 24th in the silent backlog.

## Today's row
- slug: `gen-2026-08-21-in9gt`
- angle_tag: `spelling-bee-audience-throat-lozenge-rec`
- display: "She named the throat lozenges / in the spelling bee audience."
- status: `fb_published` (expected `ig_published`)
- fb_post_id: `1086292991223812_122120670470989743` (fb_published_at 14:01:12 UTC)
- ig_media_id: null
- ig_published_at: null

## Vercel runtime logs (2026-08-21)
- 14:00:42 `GET /api/social/fb-daily-generate` → 200 (only a `url.parse` DEP0169 deprecation warning on stderr)
- 14:15:08 `GET /api/social/ig-mirror` → 200 (no serverless output — early-return path)

The 200/no-output shape of the ig-mirror log is consistent with the `auth/page-context: …` early return in `app/api/social/ig-mirror/route.ts:182-192` (ok:false, no rows touched).

## Root cause
Meta account configuration, not a code bug. The IG Business Account (`17841442248205840`) is not currently linked to the FB Page (`1086292991223812`), so `GET /{page_id}?fields=instagram_business_account` returns no `instagram_business_account` field and `fetchPageContext` throws `No Instagram Business Account linked to FB Page …`. History confirms:

- Last successful IG publish: `gen-2026-05-27-p8u57` at 2026-05-27 14:15:37 UTC (ig_media_id `18080232848404779`).
- 2026-07-29 07:24:38 UTC: a prior Claude session bulk-marked 55 stranded `fb_published` rows as `ig_failed` with `skipped-stale-backlog: IG business account was unlinked from the FB Page ~2026-05-28 …` to prevent bulk-publishing stale content once the link is restored.
- Since then, 24 more `fb_published` rows have accumulated (2026-07-28 → 2026-08-21).

Status distribution today: `ig_failed` 55, `fb_published` 24 (pending), `ig_published` 11, `fb_failed` 2.

## Action required (Kevin)
1. Re-link the IG Business Account to the FB Page in Meta Business Suite (Business Settings → Accounts → Instagram accounts → connect `@momfluence` IG business account to FB Page `facebook.com/momfluence`).
2. Once relinked, decide what to do with the 24-row backlog:
   - Bulk-mark them `ig_failed` with a `skipped-stale-backlog …` note (mirror the 2026-07-29 remediation), OR
   - Let the next `ig-mirror` run publish them (IG rate-limits at 25 media publishes / 24h — the backlog is right at the ceiling).

## Live-post spot-check
Skipped WebFetch of `facebook.com/momfluence/posts` — Facebook blocks unauthenticated scrapes (403), as noted in the 2026-05-13 log. Well-formed `fb_post_id` `1086292991223812_122120670470989743` from the successful Graph API POST is the proof of publish for FB. IG cannot be spot-checked because nothing was published.

## Not fixing in code
The endpoint's current behavior — leave rows `fb_published` on shared-context auth failure so a transient Meta outage self-heals tomorrow — is intentional. The failure here is persistent, not transient; the fix is at the Meta account layer. Silent backlog accumulation is the routine's job to catch (as it did today), not the endpoint's job to prevent.

## PR
Log-only: docs/routine-logs/health-2026-08-21.md on branch `claude/fix-content-cron-2026-08-21`.
