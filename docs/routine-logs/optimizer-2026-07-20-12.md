# Optimizer tick — 2026-07-20 12:00 UTC

## Tick summary

| Field | Value |
|---|---|
| Tick ID | `492effa4-a3e8-4ebc-a994-1fb80c88ac46` |
| occurred_at | 2026-07-20 12:01:04 UTC |
| Mode | `shadow` |
| duration_ms | 1622 |
| variants_seen | 10 |
| ads_seen | 45 |
| signups_seen | 2 |
| data_pull_ok | true |

Cron is healthy — tick fired on schedule, 1.6s runtime, no error.

## Decisions

| decision_type | count |
|---|---|
| NO-OP | 10 |
| PAUSE | 0 |
| SCALE | 0 |
| REMIX | 0 |

All 10 variants returned `no_op` with rationale `Insufficient data — visits=0 (need ≥100), conv=0 (need ≥5)`. Posterior p_best is still tightly clustered around 0.10 across every variant (range 0.0957–0.1070), which is the flat prior — no learning yet.

## Status

Awaiting data — 0 visits recorded across 10 variants, posterior still flat. Day 1 of the week-1 shadow observation window; nothing for a human to review.

## Notes worth flagging (non-blocking)

- `signups_seen=2` at the tick level while every per-variant `visits=0`. That's a data-plumbing mismatch — signups are being counted but the visit-attribution join into `optimizer_actions` is not yet populating. Worth checking before we exit shadow, otherwise the sample-size gate (`visits ≥ 100`) will never open. Not urgent today.
- Variant `move-over-influencers` has `meta_ad_id=null` — the other 9 have Meta ad IDs. Either it isn't wired to Meta yet or the mapping row is missing.
